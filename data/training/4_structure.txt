Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise definition with a fraction inside absolute values
Output: \begin{cases} \left|\frac{a}{b}\right| & x > 0 \\ 0 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a vector field written as a column with components f and g
Output: \begin{pmatrix} F \\ G \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a system defining a recurrence where a sub n equals a sub n minus one plus one
Output: \begin{cases} a_n = a_{n-1} + 1 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system expressing congruence where a is congruent to b modulo n
Output: \begin{cases} a \equiv b \pmod{n} \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining divergence equals zero
Output: \begin{cases} \nabla \cdot \mathbf{F} = 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a two by one matrix containing the coordinates x and y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a table of probabilities with p of a equals one half and p of b equals one third
Output: \begin{pmatrix} P(A)=\frac{1}{2} \\ P(B)=\frac{1}{3} \end{pmatrix}

Input: a set containing the elements one two and three
Output: \{ 1, 2, 3 \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by two matrix with entries a b c d
Output: \begin{pmatrix} a & b \\ c & d \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system where x plus y equals one and x minus y equals zero
Output: \begin{cases} x + y = 1 \\ x - y = 0 \end{cases}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise probability mass function equal to p for x equals zero and one minus p otherwise
Output: \begin{cases} p & x = 0 \\ 1-p & x \ne 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: the set of all integers less than five
Output: \{ x \in \mathbb{Z} \mid x < 5 \}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a two by one vector with entries z and conjugate of z
Output: \begin{pmatrix} z \\ \overline{z} \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a column vector containing the first three partial sums
Output: \begin{pmatrix} s_1 \\ s_2 \\ s_3 \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: an augmented two by two matrix with rows one zero one and zero one zero
Output: \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 0 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a column vector containing gamma of x and zeta of x
Output: \begin{pmatrix} \Gamma(x) \\ \zeta(x) \end{pmatrix}

Input: a system defining a circle where x squared plus y squared equals r squared
Output: \begin{cases} x^2 + y^2 = r^2 \end{cases}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set containing elements a b and c
Output: \{ a, b, c \}

Input: the set of all x such that x is greater than zero
Output: \{ x \mid x > 0 \}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a one by two row matrix with entries x y
Output: \begin{pmatrix} x & y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a system of three equations x equals one y equals two and z equals three
Output: \begin{cases} x = 1 \\ y = 2 \\ z = 3 \end{cases}

Input: the set of ordered pairs x y such that x equals y
Output: \{ (x,y) \mid x = y \}

Input: a matrix with real and imaginary parts of z
Output: \begin{pmatrix} \Re(z) \\ \Im(z) \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one column matrix with entries x y
Output: \begin{pmatrix} x \\ y \end{pmatrix}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a two by one matrix with entries sum from i equals one to n of i and sum from i equals one to n of i squared
Output: \begin{pmatrix} \sum_{i=1}^n i \\ \sum_{i=1}^n i^2 \end{pmatrix}

Input: a piecewise function equal to x squared for x greater than or equal to zero and minus x otherwise
Output: \begin{cases} x^2 & x \ge 0 \\ -x & x < 0 \end{cases}

Input: a system with equations two x plus y equals three and x minus y equals one
Output: \begin{cases} 2x + y = 3 \\ x - y = 1 \end{cases}

Input: a function defined piecewise as zero for x less than zero and one for x greater than or equal to zero
Output: \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}

Input: a piecewise definition with x for x positive and x squared for x nonpositive
Output: \begin{cases} x & x > 0 \\ x^2 & x \le 0 \end{cases}

Input: the set of all integers divisible by two
Output: \{ x \in \mathbb{Z} \mid 2 \mid x \}

Input: a three by three matrix with rows a b c d e f g h i
Output: \begin{pmatrix} a & b & c \\ d & e & f \\ g & h & i \end{pmatrix}
