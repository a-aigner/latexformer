Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: the point with coordinates y then x
Output: (x,y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x squared plus y squared
Output: x^2 + y^2

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B union A
Output: P(A \cup B)

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: z plus x plus y
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of y plus x
Output: f(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the summand y plus x
Output: (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: z equals a plus b i
Output: z = a + bi

Input: the summand y plus x
Output: (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: A intersect B
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: B union A
Output: A \cup B

Input: q or p
Output: p \lor q

Input: b i plus a
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: gcd of a and b
Output: \gcd(a,b)

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: c times a times b
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: z equals a plus b i
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: beta plus alpha
Output: \alpha + \beta

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: alpha plus beta
Output: \alpha + \beta

Input: probability of B intersect A
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: B union A
Output: A \cup B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: f of x plus y
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q or p
Output: p \lor q

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: B intersect A
Output: A \cap B

Input: x squared plus y squared
Output: x^2 + y^2

Input: a plus b i
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: p or q
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B intersect A
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: gcd of b and a
Output: \gcd(a,b)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times c times a
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: y plus z plus x
Output: x + y + z

Input: beta plus alpha
Output: \alpha + \beta

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: b i plus a
Output: a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: matrix B plus matrix A
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: y plus x
Output: x + y

Input: A union B
Output: A \cup B

Input: b times c times a
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: p or q
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: the point with coordinates y then x
Output: (x,y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: A union B
Output: A \cup B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times a
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: gcd of b and a
Output: \gcd(a,b)

Input: B union A
Output: A \cup B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: A intersect B
Output: A \cap B

Input: the point with coordinates x then y
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: probability of B union A
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z equals b i plus a
Output: z = a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of y plus x
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a plus b i
Output: a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: y plus z plus x
Output: x + y + z

Input: alpha plus beta
Output: \alpha + \beta

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: b i plus a
Output: a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: b times a
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: x squared plus y squared
Output: x^2 + y^2

Input: x plus y
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix A plus matrix B
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: B union A
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: p or q
Output: p \lor q

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: q or p
Output: p \lor q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: A intersect B
Output: A \cap B

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a plus b i
Output: a + bi

Input: f of y plus x
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: probability of B intersect A
Output: P(A \cap B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: a plus b i
Output: a + bi

Input: A intersect B
Output: A \cap B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b i plus a
Output: a + bi

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: gcd of b and a
Output: \gcd(a,b)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: y squared plus x squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: b i plus a
Output: a + bi

Input: a plus b i
Output: a + bi

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: a equals b
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: B union A
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: gcd of a and b
Output: \gcd(a,b)

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals b i plus a
Output: z = a + bi

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: probability of B intersect A
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: probability of B intersect A
Output: P(A \cap B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of A union B
Output: P(A \cup B)

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: alpha plus beta
Output: \alpha + \beta

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: matrix A plus matrix B
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: b i plus a
Output: a + bi

Input: a times b
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: B intersect A
Output: A \cap B

Input: p or q
Output: p \lor q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: z equals b i plus a
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: sine of y plus x
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: A intersect B
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the summand y plus x
Output: (x + y)

Input: A union B
Output: A \cup B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: gcd of a and b
Output: \gcd(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: the point with coordinates x then y
Output: (x,y)

Input: x plus y
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: A union B
Output: A \cup B

Input: x plus y
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: q or p
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b i plus a
Output: a + bi

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix A plus matrix B
Output: A + B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: a equals b
Output: a = b

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p and q
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: b equals a
Output: a = b

Input: B intersect A
Output: A \cap B

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals a plus b i
Output: z = a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z equals b i plus a
Output: z = a + bi

Input: A intersect B
Output: A \cap B

Input: q and p
Output: p \land q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: a times b
Output: ab

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: alpha plus beta
Output: \alpha + \beta

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: a plus b i
Output: a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: y plus x
Output: x + y

Input: c times a times b
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: A union B
Output: A \cup B

Input: probability of A intersect B
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: B union A
Output: A \cup B

Input: y squared plus x squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: matrix A plus matrix B
Output: A + B

Input: B union A
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: q and p
Output: p \land q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus x
Output: x + y

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a plus b i
Output: a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x squared plus y squared
Output: x^2 + y^2

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: x plus y
Output: x + y

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: y squared plus x squared
Output: x^2 + y^2

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: gcd of b and a
Output: \gcd(a,b)

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the point with coordinates x then y
Output: (x,y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the point with coordinates x then y
Output: (x,y)

Input: b equals a
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: q or p
Output: p \lor q

Input: B union A
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of B union A
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: B intersect A
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: probability of B union A
Output: P(A \cup B)

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: A intersect B
Output: A \cap B

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: gcd of b and a
Output: \gcd(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: a plus b i
Output: a + bi

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b equals a
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of A intersect B
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: B intersect A
Output: A \cap B

Input: sine of x plus y
Output: \sin(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: A union B
Output: A \cup B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: b times c times a
Output: abc

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: z equals b i plus a
Output: z = a + bi

Input: b times a
Output: ab

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: q or p
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: x plus y
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: p and q
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: p and q
Output: p \land q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: p or q
Output: p \lor q

Input: matrix B plus matrix A
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of B union A
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: probability of B union A
Output: P(A \cup B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the summand y plus x
Output: (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: A union B
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: p or q
Output: p \lor q

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: x plus y
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: b times a
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: z equals b i plus a
Output: z = a + bi

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: alpha plus beta
Output: \alpha + \beta

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the point with coordinates x then y
Output: (x,y)

Input: b times c times a
Output: abc

Input: p or q
Output: p \lor q

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: a equals b
Output: a = b

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a plus b i
Output: a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: alpha plus beta
Output: \alpha + \beta

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix B plus matrix A
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: p or q
Output: p \lor q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q or p
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: probability of B union A
Output: P(A \cup B)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the point with coordinates x then y
Output: (x,y)

Input: c times a times b
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: a plus b i
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: q and p
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: q or p
Output: p \lor q

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: a equals b
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: A union B
Output: A \cup B

Input: alpha plus beta
Output: \alpha + \beta

Input: f of x plus y
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: p and q
Output: p \land q

Input: beta plus alpha
Output: \alpha + \beta

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus z plus x
Output: x + y + z

Input: q and p
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: the summand x plus y
Output: (x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the point with coordinates y then x
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: b times a
Output: ab

Input: a plus b i
Output: a + bi

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: q and p
Output: p \land q

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the point with coordinates x then y
Output: (x,y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: c times a times b
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: z equals a plus b i
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: z equals b i plus a
Output: z = a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: alpha plus beta
Output: \alpha + \beta

Input: b times a
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: A intersect B
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: x plus y
Output: x + y

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: A intersect B
Output: A \cap B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: a plus b i
Output: a + bi

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p or q
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the summand x plus y
Output: (x + y)

Input: A intersect B
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of B intersect A
Output: P(A \cap B)

Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: A intersect B
Output: A \cap B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: y squared plus x squared
Output: x^2 + y^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: matrix A plus matrix B
Output: A + B

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: a times b
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: y plus x
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A intersect B
Output: A \cap B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: a times b
Output: ab

Input: gcd of b and a
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: q or p
Output: p \lor q

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: p and q
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of Y plus X
Output: E[X + Y]

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: A intersect B
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a times b
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: b times c times a
Output: abc

Input: the point with coordinates y then x
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: c times a times b
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: x plus y
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the point with coordinates y then x
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: a times b
Output: ab

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of A union B
Output: P(A \cup B)

Input: c times a times b
Output: abc

Input: B union A
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: y plus z plus x
Output: x + y + z

Input: A intersect B
Output: A \cap B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix A plus matrix B
Output: A + B

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates y then x
Output: (x,y)

Input: b times c times a
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: gcd of b and a
Output: \gcd(a,b)

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: b equals a
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: q or p
Output: p \lor q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A union B
Output: P(A \cup B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: b times a
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: B intersect A
Output: A \cap B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b i plus a
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals a plus b i
Output: z = a + bi

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: B intersect A
Output: A \cap B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: q and p
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: b i plus a
Output: a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of B union A
Output: P(A \cup B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: alpha plus beta
Output: \alpha + \beta

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: a times b
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand y plus x
Output: (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: p or q
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: matrix A plus matrix B
Output: A + B

Input: the point with coordinates y then x
Output: (x,y)

Input: b equals a
Output: a = b

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A intersect B
Output: A \cap B

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b i plus a
Output: a + bi

Input: f of x plus y
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: y plus z plus x
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: the point with coordinates y then x
Output: (x,y)

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals a plus b i
Output: z = a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: q or p
Output: p \lor q

Input: y plus z plus x
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: a times b
Output: ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals b i plus a
Output: z = a + bi

Input: A union B
Output: A \cup B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: B intersect A
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of B union A
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a equals b
Output: a = b

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: p or q
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: p or q
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of A union B
Output: P(A \cup B)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B intersect A
Output: A \cap B

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: a plus b i
Output: a + bi

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a plus b i
Output: a + bi

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: a plus b i
Output: a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: b times a
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: p or q
Output: p \lor q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of B union A
Output: P(A \cup B)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: A union B
Output: A \cup B

Input: q and p
Output: p \land q

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b i plus a
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: gcd of a and b
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: matrix B plus matrix A
Output: A + B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A union B
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: f of y plus x
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b i plus a
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of a and b
Output: \gcd(a,b)

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: b times c times a
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: B union A
Output: A \cup B

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: gcd of b and a
Output: \gcd(a,b)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y plus z plus x
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: probability of A union B
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: a times b
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: the point with coordinates x then y
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: a plus b i
Output: a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: f of y plus x
Output: f(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the summand y plus x
Output: (x + y)

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: z equals b i plus a
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: B union A
Output: A \cup B

Input: gcd of a and b
Output: \gcd(a,b)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: the point with coordinates y then x
Output: (x,y)

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: B intersect A
Output: A \cap B

Input: gcd of a and b
Output: \gcd(a,b)

Input: z equals b i plus a
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: B intersect A
Output: A \cap B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: b equals a
Output: a = b

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: f of y plus x
Output: f(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b equals a
Output: a = b

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: c times a times b
Output: abc

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates y then x
Output: (x,y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the summand x plus y
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: A intersect B
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: z equals b i plus a
Output: z = a + bi

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: beta plus alpha
Output: \alpha + \beta

Input: f of x plus y
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of X plus Y
Output: E[X + Y]

Input: A union B
Output: A \cup B

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of B union A
Output: P(A \cup B)

Input: z equals a plus b i
Output: z = a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z equals b i plus a
Output: z = a + bi

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q and p
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: x plus y
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: q or p
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: probability of B union A
Output: P(A \cup B)

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: a equals b
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: B union A
Output: A \cup B

Input: a equals b
Output: a = b

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: p or q
Output: p \lor q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: B intersect A
Output: A \cap B

Input: c times a times b
Output: abc

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: c times a times b
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the summand x plus y
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a plus b i
Output: a + bi

Input: f of y plus x
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B union A
Output: P(A \cup B)

Input: b equals a
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: y squared plus x squared
Output: x^2 + y^2

Input: a times b
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: gcd of a and b
Output: \gcd(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: a times b
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: gcd of b and a
Output: \gcd(a,b)

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: matrix A plus matrix B
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: probability of A union B
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: the point with coordinates x then y
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of A union B
Output: P(A \cup B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: sine of y plus x
Output: \sin(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: p or q
Output: p \lor q

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: b times c times a
Output: abc

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: y plus z plus x
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand y plus x
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: p or q
Output: p \lor q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: y plus x
Output: x + y

Input: z equals b i plus a
Output: z = a + bi

Input: the summand x plus y
Output: (x + y)

Input: a times b
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates y then x
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: sine of x plus y
Output: \sin(x + y)

Input: a times b
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: A intersect B
Output: A \cap B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: a equals b
Output: a = b

Input: beta plus alpha
Output: \alpha + \beta

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: probability of B union A
Output: P(A \cup B)

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B union A
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b equals a
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the summand x plus y
Output: (x + y)

Input: q or p
Output: p \lor q

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: probability of A union B
Output: P(A \cup B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q and p
Output: p \land q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand y plus x
Output: (x + y)

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: A intersect B
Output: A \cap B

Input: b times a
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: c times a times b
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: x squared plus y squared
Output: x^2 + y^2

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: matrix B plus matrix A
Output: A + B

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: b equals a
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: x squared plus y squared
Output: x^2 + y^2

Input: B intersect A
Output: A \cap B

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: probability of A intersect B
Output: P(A \cap B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: B union A
Output: A \cup B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p or q
Output: p \lor q

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: B union A
Output: A \cup B

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: probability of A union B
Output: P(A \cup B)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: B union A
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of B union A
Output: P(A \cup B)

Input: A intersect B
Output: A \cap B

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: beta plus alpha
Output: \alpha + \beta

Input: matrix A plus matrix B
Output: A + B

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p or q
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B union A
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix A plus matrix B
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: x plus y
Output: x + y

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times c times a
Output: abc

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: b equals a
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: a plus b i
Output: a + bi

Input: p or q
Output: p \lor q

Input: expected value of Y plus X
Output: E[X + Y]

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x squared plus y squared
Output: x^2 + y^2

Input: gcd of b and a
Output: \gcd(a,b)

Input: probability of A union B
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: a times b
Output: ab

Input: q and p
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: p or q
Output: p \lor q

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p and q
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z equals a plus b i
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: A union B
Output: A \cup B

Input: probability of A union B
Output: P(A \cup B)

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B intersect A
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times c times a
Output: abc

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: p and q
Output: p \land q

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: beta plus alpha
Output: \alpha + \beta

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: B union A
Output: A \cup B

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: a times b
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: B union A
Output: A \cup B

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: beta plus alpha
Output: \alpha + \beta

Input: y squared plus x squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q or p
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: A intersect B
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: z equals b i plus a
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates x then y
Output: (x,y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b equals a
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a plus b i
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: b times a
Output: ab

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: b times c times a
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q and p
Output: p \land q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the point with coordinates y then x
Output: (x,y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: B union A
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: x plus y
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: B union A
Output: A \cup B

Input: p and q
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: p and q
Output: p \land q

Input: a plus b i
Output: a + bi

Input: p and q
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: B union A
Output: A \cup B

Input: z equals a plus b i
Output: z = a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z plus x plus y
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: z equals b i plus a
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B intersect A
Output: A \cap B

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: a times b
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: gcd of a and b
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: gcd of a and b
Output: \gcd(a,b)

Input: b equals a
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: alpha plus beta
Output: \alpha + \beta

Input: b i plus a
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix A plus matrix B
Output: A + B

Input: alpha plus beta
Output: \alpha + \beta

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: f of y plus x
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: gcd of b and a
Output: \gcd(a,b)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: a equals b
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: c times a times b
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: A union B
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: beta plus alpha
Output: \alpha + \beta

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: A intersect B
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: A intersect B
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: a times b
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: b times a
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates y then x
Output: (x,y)

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: p and q
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: B intersect A
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: B union A
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q or p
Output: p \lor q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: a times b
Output: ab

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: p and q
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: the point with coordinates y then x
Output: (x,y)

Input: c times a times b
Output: abc

Input: q or p
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: probability of A intersect B
Output: P(A \cap B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: gcd of b and a
Output: \gcd(a,b)

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q or p
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: probability of A union B
Output: P(A \cup B)

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: b i plus a
Output: a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: a times b
Output: ab

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: B intersect A
Output: A \cap B

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: beta plus alpha
Output: \alpha + \beta

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: B intersect A
Output: A \cap B

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: beta plus alpha
Output: \alpha + \beta

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: gcd of b and a
Output: \gcd(a,b)

Input: B union A
Output: A \cup B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: A union B
Output: A \cup B

Input: y squared plus x squared
Output: x^2 + y^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand y plus x
Output: (x + y)

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: expected value of Y plus X
Output: E[X + Y]

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: probability of A union B
Output: P(A \cup B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A union B
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: beta plus alpha
Output: \alpha + \beta

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A union B
Output: P(A \cup B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the summand y plus x
Output: (x + y)

Input: y plus x
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: expected value of X plus Y
Output: E[X + Y]

Input: matrix B plus matrix A
Output: A + B

Input: x plus y
Output: x + y

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: gcd of b and a
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: a plus b i
Output: a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: x plus y
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B union A
Output: A \cup B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the point with coordinates x then y
Output: (x,y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: c times a times b
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: gcd of b and a
Output: \gcd(a,b)

Input: the summand y plus x
Output: (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: expected value of Y plus X
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: z equals b i plus a
Output: z = a + bi

Input: b times c times a
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the point with coordinates x then y
Output: (x,y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus x
Output: x + y

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: b times a
Output: ab

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix A plus matrix B
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: A intersect B
Output: A \cap B

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: y squared plus x squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b equals a
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q or p
Output: p \lor q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B union A
Output: A \cup B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b i plus a
Output: a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: the point with coordinates x then y
Output: (x,y)

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: q or p
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: alpha plus beta
Output: \alpha + \beta

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: a plus b i
Output: a + bi

Input: the summand y plus x
Output: (x + y)

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: q and p
Output: p \land q

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: a times b
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: b i plus a
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y squared plus x squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B union A
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: probability of A intersect B
Output: P(A \cap B)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: A union B
Output: A \cup B

Input: q and p
Output: p \land q

Input: b times c times a
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of x plus y
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: y plus z plus x
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B union A
Output: A \cup B

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: a plus b i
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: B union A
Output: A \cup B

Input: a plus b i
Output: a + bi

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: expected value of Y plus X
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: beta plus alpha
Output: \alpha + \beta

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the point with coordinates y then x
Output: (x,y)

Input: q or p
Output: p \lor q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: probability of A union B
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: a equals b
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: p or q
Output: p \lor q

Input: b times c times a
Output: abc

Input: q or p
Output: p \lor q

Input: a times b
Output: ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: p or q
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: B intersect A
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: B intersect A
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: gcd of a and b
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: z equals b i plus a
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: A intersect B
Output: A \cap B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: B intersect A
Output: A \cap B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: q or p
Output: p \lor q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: alpha plus beta
Output: \alpha + \beta

Input: B intersect A
Output: A \cap B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: A intersect B
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of B union A
Output: P(A \cup B)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: a equals b
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: f of x plus y
Output: f(x + y)

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: y plus x
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: expected value of X plus Y
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: beta plus alpha
Output: \alpha + \beta

Input: B intersect A
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: q and p
Output: p \land q

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: gcd of a and b
Output: \gcd(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: f of y plus x
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: a equals b
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: p or q
Output: p \lor q

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: alpha plus beta
Output: \alpha + \beta

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: B intersect A
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: f of y plus x
Output: f(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times c times a
Output: abc

Input: A intersect B
Output: A \cap B

Input: b equals a
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: expected value of Y plus X
Output: E[X + Y]

Input: B union A
Output: A \cup B

Input: c times a times b
Output: abc

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: A union B
Output: A \cup B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: alpha plus beta
Output: \alpha + \beta

Input: z equals a plus b i
Output: z = a + bi

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: probability of A intersect B
Output: P(A \cap B)

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: alpha plus beta
Output: \alpha + \beta

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of X plus Y
Output: E[X + Y]

Input: B intersect A
Output: A \cap B

Input: beta plus alpha
Output: \alpha + \beta

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: x plus y
Output: x + y

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand y plus x
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: probability of B union A
Output: P(A \cup B)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b times a
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: b equals a
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals b i plus a
Output: z = a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B intersect A
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: a times b
Output: ab

Input: y plus x
Output: x + y

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the point with coordinates y then x
Output: (x,y)

Input: b i plus a
Output: a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: a times b
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B intersect A
Output: A \cap B

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: z plus x plus y
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the point with coordinates y then x
Output: (x,y)

Input: b i plus a
Output: a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: A intersect B
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: the point with coordinates x then y
Output: (x,y)

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: A union B
Output: A \cup B

Input: a plus b i
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times c times a
Output: abc

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: x plus y
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: probability of B union A
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: y plus x
Output: x + y

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: A intersect B
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: the summand x plus y
Output: (x + y)

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: c times a times b
Output: abc

Input: p and q
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p or q
Output: p \lor q

Input: p or q
Output: p \lor q

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: q or p
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: expected value of X plus Y
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: gcd of a and b
Output: \gcd(a,b)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: x plus y
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: A intersect B
Output: A \cap B

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b i plus a
Output: a + bi

Input: p or q
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix A plus matrix B
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: p and q
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: p and q
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: probability of B union A
Output: P(A \cup B)

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: a equals b
Output: a = b

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z plus x plus y
Output: x + y + z

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: A intersect B
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: p and q
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: the summand x plus y
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: p and q
Output: p \land q

Input: the summand y plus x
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: sine of y plus x
Output: \sin(x + y)

Input: q or p
Output: p \lor q

Input: alpha plus beta
Output: \alpha + \beta

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times c times a
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: x squared plus y squared
Output: x^2 + y^2

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: probability of B union A
Output: P(A \cup B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: B intersect A
Output: A \cap B

Input: x plus y
Output: x + y

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand x plus y
Output: (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z equals a plus b i
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: p or q
Output: p \lor q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: z equals b i plus a
Output: z = a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: y plus x
Output: x + y

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: A union B
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z equals b i plus a
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the point with coordinates x then y
Output: (x,y)

Input: B union A
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the point with coordinates x then y
Output: (x,y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: B intersect A
Output: A \cap B

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: A union B
Output: A \cup B

Input: z equals b i plus a
Output: z = a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: a plus b i
Output: a + bi

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals b i plus a
Output: z = a + bi

Input: a plus b i
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: a equals b
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: b times c times a
Output: abc

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: p or q
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the point with coordinates x then y
Output: (x,y)

Input: z equals b i plus a
Output: z = a + bi

Input: z equals b i plus a
Output: z = a + bi

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: c times a times b
Output: abc

Input: B intersect A
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: A intersect B
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: expected value of Y plus X
Output: E[X + Y]

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times c times a
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: beta plus alpha
Output: \alpha + \beta

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: probability of A intersect B
Output: P(A \cap B)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: f of y plus x
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: matrix B plus matrix A
Output: A + B

Input: c times a times b
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: x plus y
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix A plus matrix B
Output: A + B

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z equals b i plus a
Output: z = a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: b equals a
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z equals a plus b i
Output: z = a + bi

Input: b i plus a
Output: a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: A union B
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of y plus x
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates y then x
Output: (x,y)

Input: q and p
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: beta plus alpha
Output: \alpha + \beta

Input: a times b
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: gcd of b and a
Output: \gcd(a,b)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times a
Output: ab

Input: a plus b i
Output: a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: p or q
Output: p \lor q

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: x plus y
Output: x + y

Input: z equals b i plus a
Output: z = a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: f of y plus x
Output: f(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of x plus y
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: a plus b i
Output: a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: q or p
Output: p \lor q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: beta plus alpha
Output: \alpha + \beta

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the summand y plus x
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: x plus y
Output: x + y

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: x plus y
Output: x + y

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: x squared plus y squared
Output: x^2 + y^2

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z plus x plus y
Output: x + y + z

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: A union B
Output: A \cup B

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: probability of B union A
Output: P(A \cup B)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: A intersect B
Output: A \cap B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b i plus a
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: gcd of a and b
Output: \gcd(a,b)

Input: y squared plus x squared
Output: x^2 + y^2

Input: A union B
Output: A \cup B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: b times a
Output: ab

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: b times a
Output: ab

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: z equals b i plus a
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: B intersect A
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: b times c times a
Output: abc

Input: x plus y
Output: x + y

Input: b times c times a
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: b times a
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: probability of B intersect A
Output: P(A \cap B)

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: a times b
Output: ab

Input: b i plus a
Output: a + bi

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: B intersect A
Output: A \cap B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: p or q
Output: p \lor q

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a plus b i
Output: a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: matrix B plus matrix A
Output: A + B

Input: b times c times a
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix A plus matrix B
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the point with coordinates y then x
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: p or q
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B intersect A
Output: A \cap B

Input: q and p
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: b times c times a
Output: abc

Input: B union A
Output: A \cup B

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a times b
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of x plus y
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: A union B
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: q or p
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: B union A
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: q or p
Output: p \lor q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: f of y plus x
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: B union A
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: beta plus alpha
Output: \alpha + \beta

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: q and p
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: p or q
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: z equals b i plus a
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: b times a
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a plus b i
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: probability of A union B
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: b equals a
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: the summand x plus y
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: B intersect A
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: matrix B plus matrix A
Output: A + B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: B intersect A
Output: A \cap B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q or p
Output: p \lor q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: B intersect A
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: B union A
Output: A \cup B

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals b i plus a
Output: z = a + bi

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: B intersect A
Output: A \cap B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times c times a
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: a plus b i
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: B union A
Output: A \cup B

Input: f of y plus x
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates y then x
Output: (x,y)

Input: q and p
Output: p \land q

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates x then y
Output: (x,y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: x squared plus y squared
Output: x^2 + y^2

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p or q
Output: p \lor q

Input: A intersect B
Output: A \cap B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of Y plus X
Output: E[X + Y]

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: alpha plus beta
Output: \alpha + \beta

Input: y plus z plus x
Output: x + y + z

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: probability of B union A
Output: P(A \cup B)

Input: q and p
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: B union A
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: p and q
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: b i plus a
Output: a + bi

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: alpha plus beta
Output: \alpha + \beta

Input: b times c times a
Output: abc

Input: the point with coordinates y then x
Output: (x,y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: c times a times b
Output: abc

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: A union B
Output: A \cup B

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: a plus b i
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: B union A
Output: A \cup B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: y plus x
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: z equals b i plus a
Output: z = a + bi

Input: expected value of Y plus X
Output: E[X + Y]

Input: a times b
Output: ab

Input: y plus x
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q or p
Output: p \lor q

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: alpha plus beta
Output: \alpha + \beta

Input: B intersect A
Output: A \cap B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a plus b i
Output: a + bi

Input: x squared plus y squared
Output: x^2 + y^2

Input: b i plus a
Output: a + bi

Input: x plus y
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of x plus y
Output: f(x + y)

Input: b times c times a
Output: abc

Input: y squared plus x squared
Output: x^2 + y^2

Input: c times a times b
Output: abc

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: c times a times b
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals a plus b i
Output: z = a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: A intersect B
Output: A \cap B

Input: the summand y plus x
Output: (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b i plus a
Output: a + bi

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: a equals b
Output: a = b

Input: beta plus alpha
Output: \alpha + \beta

Input: q and p
Output: p \land q

Input: b i plus a
Output: a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: probability of B intersect A
Output: P(A \cap B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: x plus y
Output: x + y

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix A plus matrix B
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b i plus a
Output: a + bi

Input: matrix A plus matrix B
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals a plus b i
Output: z = a + bi

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: gcd of b and a
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: b times c times a
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b i plus a
Output: a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: b equals a
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: p or q
Output: p \lor q

Input: the point with coordinates y then x
Output: (x,y)

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: gcd of a and b
Output: \gcd(a,b)

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z equals a plus b i
Output: z = a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: alpha plus beta
Output: \alpha + \beta

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: a times b
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: f of y plus x
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B union A
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: q or p
Output: p \lor q

Input: matrix A plus matrix B
Output: A + B

Input: b times a
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the point with coordinates y then x
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: p and q
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: the point with coordinates y then x
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: b times c times a
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: probability of A union B
Output: P(A \cup B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: q or p
Output: p \lor q

Input: a times b
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: B intersect A
Output: A \cap B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: probability of B union A
Output: P(A \cup B)

Input: B intersect A
Output: A \cap B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times c times a
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: p or q
Output: p \lor q

Input: b equals a
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: y squared plus x squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p or q
Output: p \lor q

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: y plus x
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: y squared plus x squared
Output: x^2 + y^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: b i plus a
Output: a + bi

Input: B union A
Output: A \cup B

Input: z equals b i plus a
Output: z = a + bi

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: B union A
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: A intersect B
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: gcd of b and a
Output: \gcd(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: q or p
Output: p \lor q

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the summand y plus x
Output: (x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: A intersect B
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: probability of A intersect B
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: b times c times a
Output: abc

Input: gcd of a and b
Output: \gcd(a,b)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: probability of B union A
Output: P(A \cup B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates x then y
Output: (x,y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gcd of b and a
Output: \gcd(a,b)

Input: y plus x
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: the point with coordinates y then x
Output: (x,y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates y then x
Output: (x,y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: probability of A union B
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: a plus b i
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: a times b
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: p and q
Output: p \land q

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: sine of y plus x
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: p and q
Output: p \land q

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: x squared plus y squared
Output: x^2 + y^2

Input: p and q
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: q or p
Output: p \lor q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: b i plus a
Output: a + bi

Input: b equals a
Output: a = b

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates y then x
Output: (x,y)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the summand y plus x
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: p and q
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: a plus b i
Output: a + bi

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: B intersect A
Output: A \cap B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: p and q
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of B union A
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: gcd of a and b
Output: \gcd(a,b)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a plus b i
Output: a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: b times a
Output: ab

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the point with coordinates x then y
Output: (x,y)

Input: z equals b i plus a
Output: z = a + bi

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus x
Output: x + y

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: y plus x
Output: x + y

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: q or p
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: c times a times b
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand y plus x
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: B union A
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: a equals b
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: x plus y
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B intersect A
Output: P(A \cap B)

Input: sine of x plus y
Output: \sin(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: f of y plus x
Output: f(x + y)

Input: p and q
Output: p \land q

Input: y squared plus x squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of B union A
Output: P(A \cup B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: a times b
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: a plus b i
Output: a + bi

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: gcd of a and b
Output: \gcd(a,b)

Input: gcd of a and b
Output: \gcd(a,b)

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: expected value of Y plus X
Output: E[X + Y]

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z plus x plus y
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: the summand y plus x
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: p or q
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: a equals b
Output: a = b

Input: A intersect B
Output: A \cap B

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: b times c times a
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: the point with coordinates y then x
Output: (x,y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: b i plus a
Output: a + bi

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: b times c times a
Output: abc

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b times a
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: A intersect B
Output: A \cap B

Input: A union B
Output: A \cup B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: A intersect B
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: q and p
Output: p \land q

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: beta plus alpha
Output: \alpha + \beta

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: b i plus a
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: p and q
Output: p \land q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: z equals b i plus a
Output: z = a + bi

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: a plus b i
Output: a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: A union B
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: beta plus alpha
Output: \alpha + \beta

Input: y plus x
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: b times c times a
Output: abc

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates y then x
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q or p
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: B intersect A
Output: A \cap B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z equals a plus b i
Output: z = a + bi

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix A plus matrix B
Output: A + B

Input: q and p
Output: p \land q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q or p
Output: p \lor q

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: A intersect B
Output: A \cap B

Input: beta plus alpha
Output: \alpha + \beta

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: probability of B intersect A
Output: P(A \cap B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: y plus x
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates x then y
Output: (x,y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: a plus b i
Output: a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: matrix B plus matrix A
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: c times a times b
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: p and q
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b equals a
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: expected value of X plus Y
Output: E[X + Y]

Input: z equals b i plus a
Output: z = a + bi

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: y plus x
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z plus x plus y
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p or q
Output: p \lor q

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: b equals a
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B union A
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: p or q
Output: p \lor q

Input: q and p
Output: p \land q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: p or q
Output: p \lor q

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix A plus matrix B
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: q and p
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: the point with coordinates y then x
Output: (x,y)

Input: a times b
Output: ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: p or q
Output: p \lor q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: y squared plus x squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: x squared plus y squared
Output: x^2 + y^2

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: A intersect B
Output: A \cap B

Input: the point with coordinates y then x
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: c times a times b
Output: abc

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: y squared plus x squared
Output: x^2 + y^2

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z equals b i plus a
Output: z = a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: B intersect A
Output: A \cap B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a plus b i
Output: a + bi

Input: q and p
Output: p \land q

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: B intersect A
Output: A \cap B

Input: q or p
Output: p \lor q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: p or q
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: x plus y
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: f of x plus y
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: c times a times b
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: probability of A union B
Output: P(A \cup B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: B intersect A
Output: A \cap B

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: a plus b i
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: p and q
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: B union A
Output: A \cup B

Input: matrix A plus matrix B
Output: A + B

Input: p or q
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: A intersect B
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: A intersect B
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: expected value of Y plus X
Output: E[X + Y]

Input: b i plus a
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a plus b i
Output: a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: p or q
Output: p \lor q

Input: b i plus a
Output: a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: a plus b i
Output: a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: p or q
Output: p \lor q

Input: A union B
Output: A \cup B

Input: B union A
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of y plus x
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: matrix B plus matrix A
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: B intersect A
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: c times a times b
Output: abc

Input: p or q
Output: p \lor q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: probability of A union B
Output: P(A \cup B)

Input: c times a times b
Output: abc

Input: probability of A union B
Output: P(A \cup B)

Input: b i plus a
Output: a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q and p
Output: p \land q

Input: q and p
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: b equals a
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: a plus b i
Output: a + bi

Input: a times b
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b i plus a
Output: a + bi

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y plus z plus x
Output: x + y + z

Input: p and q
Output: p \land q

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: x plus y
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: probability of A union B
Output: P(A \cup B)

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: x plus y
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: expected value of Y plus X
Output: E[X + Y]

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a times b
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: a times b
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: p and q
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: probability of A intersect B
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand x plus y
Output: (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a plus b i
Output: a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the point with coordinates x then y
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: expected value of X plus Y
Output: E[X + Y]

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: matrix A plus matrix B
Output: A + B

Input: z equals b i plus a
Output: z = a + bi

Input: a equals b
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: gcd of b and a
Output: \gcd(a,b)

Input: b i plus a
Output: a + bi

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: b i plus a
Output: a + bi

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of x plus y
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: y squared plus x squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates x then y
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: q and p
Output: p \land q

Input: B intersect A
Output: A \cap B

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: gcd of b and a
Output: \gcd(a,b)

Input: q or p
Output: p \lor q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: q and p
Output: p \land q

Input: probability of A intersect B
Output: P(A \cap B)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: A intersect B
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: probability of B intersect A
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates x then y
Output: (x,y)

Input: a plus b i
Output: a + bi

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A union B
Output: P(A \cup B)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: beta plus alpha
Output: \alpha + \beta

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: A intersect B
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a plus b i
Output: a + bi

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b i plus a
Output: a + bi

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: p and q
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: probability of B intersect A
Output: P(A \cap B)

Input: matrix A plus matrix B
Output: A + B

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: matrix A plus matrix B
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: A union B
Output: A \cup B

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals a plus b i
Output: z = a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: p and q
Output: p \land q

Input: the point with coordinates x then y
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of b and a
Output: \gcd(a,b)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: probability of A intersect B
Output: P(A \cap B)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: p or q
Output: p \lor q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b equals a
Output: a = b

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: z equals b i plus a
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: y plus x
Output: x + y

Input: q or p
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: a times b
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: p and q
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B union A
Output: P(A \cup B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: q or p
Output: p \lor q

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b i plus a
Output: a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: p or q
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: q or p
Output: p \lor q

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: q or p
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: y plus x
Output: x + y

Input: p or q
Output: p \lor q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a plus b i
Output: a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: a equals b
Output: a = b

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: matrix B plus matrix A
Output: A + B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: p or q
Output: p \lor q

Input: A intersect B
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: p or q
Output: p \lor q

Input: probability of B union A
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: the summand y plus x
Output: (x + y)

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: q and p
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: probability of B intersect A
Output: P(A \cap B)

Input: a times b
Output: ab

Input: a equals b
Output: a = b

Input: B intersect A
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: alpha plus beta
Output: \alpha + \beta

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix B plus matrix A
Output: A + B

Input: c times a times b
Output: abc

Input: b times c times a
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix A plus matrix B
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals b i plus a
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: p and q
Output: p \land q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: x plus y
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B union A
Output: A \cup B

Input: q or p
Output: p \lor q

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: alpha plus beta
Output: \alpha + \beta

Input: the summand y plus x
Output: (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: B union A
Output: A \cup B

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: probability of B union A
Output: P(A \cup B)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: b i plus a
Output: a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of b and a
Output: \gcd(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: gcd of a and b
Output: \gcd(a,b)

Input: b equals a
Output: a = b

Input: x plus y
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: z equals b i plus a
Output: z = a + bi

Input: probability of A union B
Output: P(A \cup B)

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q or p
Output: p \lor q

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: A intersect B
Output: A \cap B

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A union B
Output: P(A \cup B)

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: q and p
Output: p \land q

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: p and q
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p or q
Output: p \lor q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: A union B
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: y plus x
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: q or p
Output: p \lor q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals a plus b i
Output: z = a + bi

Input: B intersect A
Output: A \cap B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of A union B
Output: P(A \cup B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: A union B
Output: A \cup B

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: b times c times a
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x plus y
Output: x + y

Input: p or q
Output: p \lor q

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p or q
Output: p \lor q

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: p or q
Output: p \lor q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: a equals b
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b times c times a
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: B union A
Output: A \cup B

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: z plus x plus y
Output: x + y + z

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: z equals b i plus a
Output: z = a + bi

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: gcd of a and b
Output: \gcd(a,b)

Input: A union B
Output: A \cup B

Input: A intersect B
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: y plus x
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q or p
Output: p \lor q

Input: expected value of X plus Y
Output: E[X + Y]

Input: expected value of X plus Y
Output: E[X + Y]

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: q or p
Output: p \lor q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: b times c times a
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of A union B
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: B intersect A
Output: A \cap B

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: c times a times b
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p or q
Output: p \lor q

Input: p and q
Output: p \land q

Input: beta plus alpha
Output: \alpha + \beta

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: a plus b i
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: q or p
Output: p \lor q

Input: gcd of a and b
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: probability of A union B
Output: P(A \cup B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: A intersect B
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: q and p
Output: p \land q

Input: a plus b i
Output: a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: p or q
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: p and q
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: p and q
Output: p \land q

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z equals a plus b i
Output: z = a + bi

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: b times c times a
Output: abc

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: b i plus a
Output: a + bi

Input: b i plus a
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: x squared plus y squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: a plus b i
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B union A
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B intersect A
Output: P(A \cap B)

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: b i plus a
Output: a + bi

Input: probability of A union B
Output: P(A \cup B)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: p and q
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b i plus a
Output: a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: a plus b i
Output: a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: y plus x
Output: x + y

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: A intersect B
Output: A \cap B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: A union B
Output: A \cup B

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b i plus a
Output: a + bi

Input: b times c times a
Output: abc

Input: B intersect A
Output: A \cap B

Input: y squared plus x squared
Output: x^2 + y^2

Input: z equals b i plus a
Output: z = a + bi

Input: A intersect B
Output: A \cap B

Input: y plus x
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: p and q
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: y squared plus x squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: x plus y
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: y squared plus x squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: z equals a plus b i
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a plus b i
Output: a + bi

Input: f of y plus x
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand y plus x
Output: (x + y)

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: A union B
Output: A \cup B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of x plus y
Output: f(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: x plus y
Output: x + y

Input: b i plus a
Output: a + bi

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: z equals a plus b i
Output: z = a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p or q
Output: p \lor q

Input: A intersect B
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand y plus x
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: alpha plus beta
Output: \alpha + \beta

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of x plus y
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: expected value of Y plus X
Output: E[X + Y]

Input: expected value of X plus Y
Output: E[X + Y]

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand y plus x
Output: (x + y)

Input: b i plus a
Output: a + bi

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: q and p
Output: p \land q

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: B union A
Output: A \cup B

Input: expected value of X plus Y
Output: E[X + Y]

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the summand y plus x
Output: (x + y)

Input: p or q
Output: p \lor q

Input: B union A
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: A union B
Output: A \cup B

Input: A union B
Output: A \cup B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: z equals b i plus a
Output: z = a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: a plus b i
Output: a + bi

Input: c times a times b
Output: abc

Input: p or q
Output: p \lor q

Input: x squared plus y squared
Output: x^2 + y^2

Input: a plus b i
Output: a + bi

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: B intersect A
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: a plus b i
Output: a + bi

Input: A union B
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: a equals b
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates y then x
Output: (x,y)

Input: b equals a
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: c times a times b
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: p or q
Output: p \lor q

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: x plus y
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: y plus z plus x
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: a plus b i
Output: a + bi

Input: c times a times b
Output: abc

Input: p or q
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: p or q
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: B intersect A
Output: A \cap B

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: a plus b i
Output: a + bi

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B union A
Output: A \cup B

Input: c times a times b
Output: abc

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b i plus a
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A intersect B
Output: P(A \cap B)

Input: B union A
Output: A \cup B

Input: a plus b i
Output: a + bi

Input: matrix B plus matrix A
Output: A + B

Input: x plus y
Output: x + y

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: a equals b
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B union A
Output: A \cup B

Input: probability of A intersect B
Output: P(A \cap B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A union B
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: q or p
Output: p \lor q

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: alpha plus beta
Output: \alpha + \beta

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b times c times a
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: alpha plus beta
Output: \alpha + \beta

Input: the summand x plus y
Output: (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: a equals b
Output: a = b

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: A intersect B
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: alpha plus beta
Output: \alpha + \beta

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: q or p
Output: p \lor q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: a plus b i
Output: a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the summand y plus x
Output: (x + y)

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: y squared plus x squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the summand y plus x
Output: (x + y)

Input: b times c times a
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: x plus y
Output: x + y

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b i plus a
Output: a + bi

Input: a times b
Output: ab

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: probability of A intersect B
Output: P(A \cap B)

Input: y plus x
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: matrix A plus matrix B
Output: A + B

Input: b i plus a
Output: a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: beta plus alpha
Output: \alpha + \beta

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: f of x plus y
Output: f(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: p or q
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: p or q
Output: p \lor q

Input: b times c times a
Output: abc

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: y plus x
Output: x + y

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: a times b
Output: ab

Input: b equals a
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: A intersect B
Output: A \cap B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: probability of A union B
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: sine of x plus y
Output: \sin(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: A union B
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: probability of B union A
Output: P(A \cup B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a plus b i
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B union A
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p or q
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus x
Output: x + y

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the point with coordinates y then x
Output: (x,y)

Input: a times b
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: B intersect A
Output: A \cap B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: the point with coordinates y then x
Output: (x,y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: B intersect A
Output: A \cap B

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: a equals b
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: a times b
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: gcd of a and b
Output: \gcd(a,b)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: p and q
Output: p \land q

Input: q or p
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: A intersect B
Output: A \cap B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the point with coordinates x then y
Output: (x,y)

Input: b times c times a
Output: abc

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: B union A
Output: A \cup B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: gcd of b and a
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: c times a times b
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x squared plus y squared
Output: x^2 + y^2

Input: p or q
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: expected value of X plus Y
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: z plus x plus y
Output: x + y + z

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: a equals b
Output: a = b

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: the summand y plus x
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix A plus matrix B
Output: A + B

Input: B union A
Output: A \cup B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: B union A
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: B union A
Output: A \cup B

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a plus b i
Output: a + bi

Input: a plus b i
Output: a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: the point with coordinates y then x
Output: (x,y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: q and p
Output: p \land q

Input: q and p
Output: p \land q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: a equals b
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: matrix A plus matrix B
Output: A + B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: p and q
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a plus b i
Output: a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: A union B
Output: A \cup B

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the point with coordinates x then y
Output: (x,y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: b equals a
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a plus b i
Output: a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: sine of x plus y
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: B union A
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: b i plus a
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p and q
Output: p \land q

Input: probability of A union B
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: b times c times a
Output: abc

Input: B union A
Output: A \cup B

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: b times c times a
Output: abc

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: A union B
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b i plus a
Output: a + bi

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: probability of A union B
Output: P(A \cup B)

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: gcd of a and b
Output: \gcd(a,b)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A union B
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: z equals b i plus a
Output: z = a + bi

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: q or p
Output: p \lor q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times c times a
Output: abc

Input: p or q
Output: p \lor q

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: A intersect B
Output: A \cap B

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: b equals a
Output: a = b

Input: b i plus a
Output: a + bi

Input: c times a times b
Output: abc

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: b equals a
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: a times b
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: p or q
Output: p \lor q

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: gcd of a and b
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: probability of B union A
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a times b
Output: ab

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: A union B
Output: A \cup B

Input: q and p
Output: p \land q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: z equals b i plus a
Output: z = a + bi

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: p or q
Output: p \lor q

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: x plus y
Output: x + y

Input: q and p
Output: p \land q

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of B union A
Output: P(A \cup B)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A intersect B
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: b times a
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: b times a
Output: ab

Input: alpha plus beta
Output: \alpha + \beta

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: q or p
Output: p \lor q

Input: matrix B plus matrix A
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: b times c times a
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: a plus b i
Output: a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: B intersect A
Output: A \cap B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: z equals b i plus a
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: A intersect B
Output: A \cap B

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a plus b i
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: probability of A union B
Output: P(A \cup B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: a plus b i
Output: a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p or q
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: a plus b i
Output: a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals b i plus a
Output: z = a + bi

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b i plus a
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: B intersect A
Output: A \cap B

Input: c times a times b
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times c times a
Output: abc

Input: c times a times b
Output: abc

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: b i plus a
Output: a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z plus x plus y
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B union A
Output: P(A \cup B)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: b times c times a
Output: abc

Input: b times a
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: the summand y plus x
Output: (x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: expected value of X plus Y
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: B intersect A
Output: A \cap B

Input: probability of A intersect B
Output: P(A \cap B)

Input: b times c times a
Output: abc

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: p or q
Output: p \lor q

Input: p and q
Output: p \land q

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b i plus a
Output: a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: A intersect B
Output: A \cap B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: sine of x plus y
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: p or q
Output: p \lor q

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: b i plus a
Output: a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B union A
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: y plus x
Output: x + y

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: q or p
Output: p \lor q

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: gcd of a and b
Output: \gcd(a,b)

Input: A intersect B
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: gcd of b and a
Output: \gcd(a,b)

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix B plus matrix A
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: b times c times a
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: probability of A union B
Output: P(A \cup B)

Input: gcd of a and b
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: p and q
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: y plus x
Output: x + y

Input: gcd of a and b
Output: \gcd(a,b)

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix A plus matrix B
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the summand x plus y
Output: (x + y)

Input: a equals b
Output: a = b

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: q and p
Output: p \land q

Input: B union A
Output: A \cup B

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the point with coordinates y then x
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: y squared plus x squared
Output: x^2 + y^2

Input: q or p
Output: p \lor q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b i plus a
Output: a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: b times c times a
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p or q
Output: p \lor q

Input: y plus z plus x
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: expected value of X plus Y
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B union A
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates x then y
Output: (x,y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: beta plus alpha
Output: \alpha + \beta

Input: z equals b i plus a
Output: z = a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: p or q
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: y plus x
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: y plus x
Output: x + y

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: p or q
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B union A
Output: P(A \cup B)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: b times c times a
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: x plus y
Output: x + y

Input: p or q
Output: p \lor q

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of B union A
Output: P(A \cup B)

Input: z plus x plus y
Output: x + y + z

Input: probability of A intersect B
Output: P(A \cap B)

Input: A union B
Output: A \cup B

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: p or q
Output: p \lor q

Input: B intersect A
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the point with coordinates y then x
Output: (x,y)

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: B intersect A
Output: A \cap B

Input: y squared plus x squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: gcd of b and a
Output: \gcd(a,b)

Input: sine of y plus x
Output: \sin(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of B intersect A
Output: P(A \cap B)

Input: probability of A union B
Output: P(A \cup B)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B union A
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: gcd of a and b
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: matrix B plus matrix A
Output: A + B

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: b i plus a
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A union B
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: p and q
Output: p \land q

Input: probability of B union A
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: y plus x
Output: x + y

Input: b i plus a
Output: a + bi

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: matrix A plus matrix B
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: beta plus alpha
Output: \alpha + \beta

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: probability of A intersect B
Output: P(A \cap B)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: p and q
Output: p \land q

Input: a times b
Output: ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: B intersect A
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: b equals a
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times a
Output: ab

Input: gcd of b and a
Output: \gcd(a,b)

Input: a times b
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B intersect A
Output: A \cap B

Input: z equals b i plus a
Output: z = a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: c times a times b
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: the summand x plus y
Output: (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates x then y
Output: (x,y)

Input: B union A
Output: A \cup B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: q or p
Output: p \lor q

Input: p or q
Output: p \lor q

Input: b times a
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: p or q
Output: p \lor q

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: b times c times a
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: probability of A union B
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: p or q
Output: p \lor q

Input: matrix B plus matrix A
Output: A + B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: p and q
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: matrix B plus matrix A
Output: A + B

Input: B union A
Output: A \cup B

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: alpha plus beta
Output: \alpha + \beta

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: y plus x
Output: x + y

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: expected value of X plus Y
Output: E[X + Y]

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: p and q
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: q and p
Output: p \land q

Input: x squared plus y squared
Output: x^2 + y^2

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p or q
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: y plus x
Output: x + y

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: z equals a plus b i
Output: z = a + bi

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: z equals b i plus a
Output: z = a + bi

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: alpha plus beta
Output: \alpha + \beta

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b equals a
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: q or p
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the point with coordinates y then x
Output: (x,y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: y plus x
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: x plus y
Output: x + y

Input: a plus b i
Output: a + bi

Input: q and p
Output: p \land q

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A intersect B
Output: A \cap B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: gcd of a and b
Output: \gcd(a,b)

Input: f of y plus x
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: f of x plus y
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: z equals a plus b i
Output: z = a + bi

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b times a
Output: ab

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals a plus b i
Output: z = a + bi

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates y then x
Output: (x,y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of y plus x
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: x plus y
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: q or p
Output: p \lor q

Input: matrix A plus matrix B
Output: A + B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a plus b i
Output: a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a plus b i
Output: a + bi

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: A union B
Output: A \cup B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: p or q
Output: p \lor q

Input: a plus b i
Output: a + bi

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q or p
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p or q
Output: p \lor q

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: A intersect B
Output: A \cap B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: B union A
Output: A \cup B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the summand y plus x
Output: (x + y)

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: sine of y plus x
Output: \sin(x + y)

Input: A intersect B
Output: A \cap B

Input: c times a times b
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: B intersect A
Output: A \cap B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: b i plus a
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: probability of B union A
Output: P(A \cup B)

Input: b i plus a
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: the point with coordinates x then y
Output: (x,y)

Input: f of x plus y
Output: f(x + y)

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: beta plus alpha
Output: \alpha + \beta

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: z plus x plus y
Output: x + y + z

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: matrix B plus matrix A
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of A union B
Output: P(A \cup B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: a equals b
Output: a = b

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a plus b i
Output: a + bi

Input: f of x plus y
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the summand x plus y
Output: (x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: a equals b
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: A intersect B
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: q or p
Output: p \lor q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: b equals a
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the point with coordinates x then y
Output: (x,y)

Input: c times a times b
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: p or q
Output: p \lor q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: expected value of Y plus X
Output: E[X + Y]

Input: b equals a
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: p or q
Output: p \lor q

Input: p or q
Output: p \lor q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: A union B
Output: A \cup B

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B intersect A
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: A intersect B
Output: A \cap B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: z equals b i plus a
Output: z = a + bi

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: z equals a plus b i
Output: z = a + bi

Input: y squared plus x squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: B union A
Output: A \cup B

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of B intersect A
Output: P(A \cap B)

Input: q or p
Output: p \lor q

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: sine of x plus y
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: A union B
Output: A \cup B

Input: z plus x plus y
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: b i plus a
Output: a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: a plus b i
Output: a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: a plus b i
Output: a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: p and q
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b i plus a
Output: a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: a plus b i
Output: a + bi

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: y squared plus x squared
Output: x^2 + y^2

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: gcd of b and a
Output: \gcd(a,b)

Input: A union B
Output: A \cup B

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a plus b i
Output: a + bi

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: b i plus a
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: a plus b i
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: z equals b i plus a
Output: z = a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: f of y plus x
Output: f(x + y)

Input: B union A
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: A union B
Output: A \cup B

Input: expected value of X plus Y
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: beta plus alpha
Output: \alpha + \beta

Input: probability of B intersect A
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of y plus x
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y squared plus x squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: B intersect A
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: f of y plus x
Output: f(x + y)

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: probability of A intersect B
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: A intersect B
Output: A \cap B

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B union A
Output: A \cup B

Input: y plus z plus x
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: q or p
Output: p \lor q

Input: c times a times b
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: A intersect B
Output: A \cap B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a times b
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: x plus y
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: c times a times b
Output: abc

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: a plus b i
Output: a + bi

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: f of y plus x
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: z equals a plus b i
Output: z = a + bi

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: matrix A plus matrix B
Output: A + B

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: B intersect A
Output: A \cap B

Input: B union A
Output: A \cup B

Input: z equals a plus b i
Output: z = a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: matrix B plus matrix A
Output: A + B

Input: y squared plus x squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: B intersect A
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: y squared plus x squared
Output: x^2 + y^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y squared plus x squared
Output: x^2 + y^2

Input: p and q
Output: p \land q

Input: q or p
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: A intersect B
Output: A \cap B

Input: p or q
Output: p \lor q

Input: q and p
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: the point with coordinates y then x
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: b i plus a
Output: a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: q or p
Output: p \lor q

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A union B
Output: P(A \cup B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: z equals b i plus a
Output: z = a + bi

Input: x plus y
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals a plus b i
Output: z = a + bi

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: alpha plus beta
Output: \alpha + \beta

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: probability of B union A
Output: P(A \cup B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of A union B
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of y plus x
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: z plus x plus y
Output: x + y + z

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: b i plus a
Output: a + bi

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: z plus x plus y
Output: x + y + z

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: c times a times b
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: p or q
Output: p \lor q

Input: B union A
Output: A \cup B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: f of y plus x
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: z equals a plus b i
Output: z = a + bi

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B intersect A
Output: A \cap B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: p and q
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: B union A
Output: A \cup B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: the point with coordinates x then y
Output: (x,y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: b times c times a
Output: abc

Input: p or q
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: the point with coordinates y then x
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: A union B
Output: A \cup B

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: f of y plus x
Output: f(x + y)

Input: A intersect B
Output: A \cap B

Input: a equals b
Output: a = b

Input: b i plus a
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: x plus y
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: a times b
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: B union A
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z equals b i plus a
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: a times b
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: p and q
Output: p \land q

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix A plus matrix B
Output: A + B

Input: probability of A union B
Output: P(A \cup B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: gcd of a and b
Output: \gcd(a,b)

Input: z plus x plus y
Output: x + y + z

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the summand x plus y
Output: (x + y)

Input: q and p
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: B union A
Output: A \cup B

Input: b equals a
Output: a = b

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q or p
Output: p \lor q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q or p
Output: p \lor q

Input: b equals a
Output: a = b

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: z plus x plus y
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: the summand x plus y
Output: (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: b equals a
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: c times a times b
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: A intersect B
Output: A \cap B

Input: a equals b
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: q and p
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: B intersect A
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: q or p
Output: p \lor q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the summand y plus x
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: matrix A plus matrix B
Output: A + B

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: b times c times a
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: beta plus alpha
Output: \alpha + \beta

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: a plus b i
Output: a + bi

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b i plus a
Output: a + bi

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p or q
Output: p \lor q

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: gcd of b and a
Output: \gcd(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: B intersect A
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: a equals b
Output: a = b

Input: probability of A intersect B
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: a equals b
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: x plus y
Output: x + y

Input: q or p
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of X plus Y
Output: E[X + Y]

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the summand y plus x
Output: (x + y)

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q or p
Output: p \lor q

Input: B union A
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: p or q
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a times b
Output: ab

Input: y plus x
Output: x + y

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b i plus a
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a plus b i
Output: a + bi

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q or p
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: B union A
Output: A \cup B

Input: matrix B plus matrix A
Output: A + B

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: c times a times b
Output: abc

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: probability of A union B
Output: P(A \cup B)

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: the point with coordinates y then x
Output: (x,y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: alpha plus beta
Output: \alpha + \beta

Input: A union B
Output: A \cup B

Input: x squared plus y squared
Output: x^2 + y^2

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: expected value of Y plus X
Output: E[X + Y]

Input: B intersect A
Output: A \cap B

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A intersect B
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q or p
Output: p \lor q

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a plus b i
Output: a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: B intersect A
Output: A \cap B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the point with coordinates x then y
Output: (x,y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y squared plus x squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of y plus x
Output: f(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: probability of A union B
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: gcd of b and a
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: a times b
Output: ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: a times b
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: b equals a
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: f of x plus y
Output: f(x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: A intersect B
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: A intersect B
Output: A \cap B

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: y plus x
Output: x + y

Input: a times b
Output: ab

Input: B intersect A
Output: A \cap B

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: A union B
Output: A \cup B

Input: the summand y plus x
Output: (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: B intersect A
Output: A \cap B

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: probability of A intersect B
Output: P(A \cap B)

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: expected value of X plus Y
Output: E[X + Y]

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z equals a plus b i
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: alpha plus beta
Output: \alpha + \beta

Input: c times a times b
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: b times a
Output: ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: A union B
Output: A \cup B

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: b times a
Output: ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: b equals a
Output: a = b

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: matrix B plus matrix A
Output: A + B

Input: c times a times b
Output: abc

Input: z equals b i plus a
Output: z = a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: alpha plus beta
Output: \alpha + \beta

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: y plus x
Output: x + y

Input: b i plus a
Output: a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the point with coordinates x then y
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z plus x plus y
Output: x + y + z

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: a plus b i
Output: a + bi

Input: B union A
Output: A \cup B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: q and p
Output: p \land q

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: sine of x plus y
Output: \sin(x + y)

Input: a plus b i
Output: a + bi

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: x plus y
Output: x + y

Input: B intersect A
Output: A \cap B

Input: A union B
Output: A \cup B

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: B intersect A
Output: A \cap B

Input: gcd of b and a
Output: \gcd(a,b)

Input: z equals a plus b i
Output: z = a + bi

Input: x plus y
Output: x + y

Input: p or q
Output: p \lor q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: A intersect B
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: x plus y
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: a equals b
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b i plus a
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: matrix A plus matrix B
Output: A + B

Input: p or q
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: z equals a plus b i
Output: z = a + bi

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: z equals a plus b i
Output: z = a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: probability of B union A
Output: P(A \cup B)

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of A union B
Output: P(A \cup B)

Input: y squared plus x squared
Output: x^2 + y^2

Input: q or p
Output: p \lor q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: sine of y plus x
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: A union B
Output: A \cup B

Input: p or q
Output: p \lor q

Input: b times a
Output: ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: gcd of a and b
Output: \gcd(a,b)

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: q and p
Output: p \land q

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: alpha plus beta
Output: \alpha + \beta

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B union A
Output: A \cup B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of x plus y
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a plus b i
Output: a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: q or p
Output: p \lor q

Input: b times c times a
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b i plus a
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: gcd of a and b
Output: \gcd(a,b)

Input: sine of x plus y
Output: \sin(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand x plus y
Output: (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand y plus x
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: gcd of b and a
Output: \gcd(a,b)

Input: probability of A union B
Output: P(A \cup B)

Input: f of x plus y
Output: f(x + y)

Input: b i plus a
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q and p
Output: p \land q

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: matrix A plus matrix B
Output: A + B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: expected value of Y plus X
Output: E[X + Y]

Input: the summand x plus y
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the point with coordinates x then y
Output: (x,y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: B union A
Output: A \cup B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: q or p
Output: p \lor q

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix A plus matrix B
Output: A + B

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a times b
Output: ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A intersect B
Output: P(A \cap B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: p or q
Output: p \lor q

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z plus x plus y
Output: x + y + z

Input: a plus b i
Output: a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: z equals b i plus a
Output: z = a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates x then y
Output: (x,y)

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A union B
Output: A \cup B

Input: matrix A plus matrix B
Output: A + B

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: a plus b i
Output: a + bi

Input: x squared plus y squared
Output: x^2 + y^2

Input: b equals a
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: gcd of b and a
Output: \gcd(a,b)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B union A
Output: P(A \cup B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of a and b
Output: \gcd(a,b)

Input: the summand x plus y
Output: (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: A intersect B
Output: A \cap B

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of y plus x
Output: f(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: B intersect A
Output: A \cap B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: c times a times b
Output: abc

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: b equals a
Output: a = b

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: a equals b
Output: a = b

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: p or q
Output: p \lor q

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the summand x plus y
Output: (x + y)

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: p or q
Output: p \lor q

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: sine of x plus y
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: probability of B intersect A
Output: P(A \cap B)

Input: x plus y
Output: x + y

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B intersect A
Output: A \cap B

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: a plus b i
Output: a + bi

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: a plus b i
Output: a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: q or p
Output: p \lor q

Input: the point with coordinates x then y
Output: (x,y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: z equals b i plus a
Output: z = a + bi

Input: the summand x plus y
Output: (x + y)

Input: q or p
Output: p \lor q

Input: a plus b i
Output: a + bi

Input: b times c times a
Output: abc

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: gcd of a and b
Output: \gcd(a,b)

Input: b i plus a
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b i plus a
Output: a + bi

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: B union A
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: b times c times a
Output: abc

Input: b times a
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the quantity y plus x in parentheses
Output: \left(x + y\right)

Input: c times a times b
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a plus b i
Output: a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: expected value of Y plus X
Output: E[X + Y]

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a plus b i
Output: a + bi

Input: b i plus a
Output: a + bi

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: beta plus alpha
Output: \alpha + \beta

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A intersect B
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: B union A
Output: A \cup B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the point with coordinates y then x
Output: (x,y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p or q
Output: p \lor q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: A intersect B
Output: A \cap B

Input: probability of A intersect B
Output: P(A \cap B)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: x plus y
Output: x + y

Input: b times c times a
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B

Input: q and p
Output: p \land q

Input: y plus x
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: probability of A union B
Output: P(A \cup B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of X plus Y
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: expected value of X plus Y
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b i plus a
Output: a + bi

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of B union A
Output: P(A \cup B)

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B union A
Output: A \cup B

Input: b times a
Output: ab

Input: B intersect A
Output: A \cap B

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: y squared plus x squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: a times b
Output: ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b times c times a
Output: abc

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a plus b i
Output: a + bi

Input: f of x plus y
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: z equals b i plus a
Output: z = a + bi

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: alpha plus beta
Output: \alpha + \beta

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: beta plus alpha
Output: \alpha + \beta

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: p and q
Output: p \land q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: expected value of X plus Y
Output: E[X + Y]

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of a and b
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of A intersect B
Output: P(A \cap B)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: q and p
Output: p \land q

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: sine of x plus y
Output: \sin(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: x plus y
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: y plus x
Output: x + y

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: A intersect B
Output: A \cap B

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: q and p
Output: p \land q

Input: y squared plus x squared
Output: x^2 + y^2

Input: b times a
Output: ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the summand x plus y
Output: (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates y then x
Output: (x,y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: z equals a plus b i
Output: z = a + bi

Input: probability of A union B
Output: P(A \cup B)

Input: q or p
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: probability of A intersect B
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: the summand x plus y
Output: (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b i plus a
Output: a + bi

Input: p or q
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: b times a
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: A intersect B
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: q and p
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand x plus y
Output: (x + y)

Input: b times a
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: c times a times b
Output: abc

Input: z plus x plus y
Output: x + y + z

Input: b equals a
Output: a = b

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B intersect A
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z equals a plus b i
Output: z = a + bi

Input: gcd of a and b
Output: \gcd(a,b)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: gcd of a and b
Output: \gcd(a,b)

Input: b times a
Output: ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the point with coordinates y then x
Output: (x,y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: B union A
Output: A \cup B

Input: gcd of a and b
Output: \gcd(a,b)

Input: p and q
Output: p \land q

Input: f of y plus x
Output: f(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: b times a
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: expected value of Y plus X
Output: E[X + Y]

Input: matrix B plus matrix A
Output: A + B

Input: q and p
Output: p \land q

Input: c times a times b
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: matrix A plus matrix B
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: x plus y
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a plus b i
Output: a + bi

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: sine of x plus y
Output: \sin(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: probability of A union B
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: matrix B plus matrix A
Output: A + B

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z equals a plus b i
Output: z = a + bi

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p and q
Output: p \land q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: z plus x plus y
Output: x + y + z

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: z equals b i plus a
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: beta plus alpha
Output: \alpha + \beta

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: q or p
Output: p \lor q

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals b i plus a
Output: z = a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: q or p
Output: p \lor q

Input: y plus x
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: A union B
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: a times b
Output: ab

Input: A intersect B
Output: A \cap B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: the summand x plus y
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: gcd of b and a
Output: \gcd(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: q or p
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: a equals b
Output: a = b

Input: q and p
Output: p \land q

Input: b times a
Output: ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: A union B
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of B union A
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: matrix A plus matrix B
Output: A + B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: q and p
Output: p \land q

Input: p or q
Output: p \lor q

Input: the point with coordinates x then y
Output: (x,y)

Input: matrix B plus matrix A
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: probability of B union A
Output: P(A \cup B)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: expected value of X plus Y
Output: E[X + Y]

Input: q and p
Output: p \land q

Input: the summand y plus x
Output: (x + y)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: f of y plus x
Output: f(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: A union B
Output: A \cup B

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: p and q
Output: p \land q

Input: expected value of Y plus X
Output: E[X + Y]

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand x plus y
Output: (x + y)

Input: a equals b
Output: a = b

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: probability of A union B
Output: P(A \cup B)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus x
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: beta plus alpha
Output: \alpha + \beta

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: B union A
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times c times a
Output: abc

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: A intersect B
Output: A \cap B

Input: the summand y plus x
Output: (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the summand y plus x
Output: (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: A union B
Output: A \cup B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the summand x plus y
Output: (x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: sine of x plus y
Output: \sin(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: p or q
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus x
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a equals b
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: A union B
Output: A \cup B

Input: B intersect A
Output: A \cap B

Input: b times a
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: a equals b
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times a
Output: ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gcd of b and a
Output: \gcd(a,b)

Input: A union B
Output: A \cup B

Input: the summand x plus y
Output: (x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: b times c times a
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of x plus y
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: sine of x plus y
Output: \sin(x + y)

Input: p and q
Output: p \land q

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the point with coordinates x then y
Output: (x,y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times a
Output: ab

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: probability of B union A
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: the point with coordinates y then x
Output: (x,y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p or q
Output: p \lor q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: c times a times b
Output: abc

Input: x squared plus y squared
Output: x^2 + y^2

Input: b times c times a
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: the point with coordinates y then x
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the summand x plus y
Output: (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p or q
Output: p \lor q

Input: expected value of X plus Y
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times c times a
Output: abc

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A intersect B
Output: A \cap B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: the summand y plus x
Output: (x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: q or p
Output: p \lor q

Input: y plus x
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of A intersect B
Output: P(A \cap B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b equals a
Output: a = b

Input: x plus y
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: y squared plus x squared
Output: x^2 + y^2

Input: z equals b i plus a
Output: z = a + bi

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: a times b
Output: ab

Input: x plus y
Output: x + y

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: y plus x
Output: x + y

Input: A intersect B
Output: A \cap B

Input: p or q
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: probability of A intersect B
Output: P(A \cap B)

Input: a times b
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: z plus x plus y
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: probability of B intersect A
Output: P(A \cap B)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: p and q
Output: p \land q

Input: B intersect A
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: a equals b
Output: a = b

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: B intersect A
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: a plus b i
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus z plus x
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a plus b i
Output: a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A intersect B
Output: A \cap B

Input: expected value of X plus Y
Output: E[X + Y]

Input: z equals a plus b i
Output: z = a + bi

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: b equals a
Output: a = b

Input: f of y plus x
Output: f(x + y)

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand x plus y
Output: (x + y)

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A union B
Output: A \cup B

Input: a times b
Output: ab

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: b times c times a
Output: abc

Input: gcd of b and a
Output: \gcd(a,b)

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates x then y
Output: (x,y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p and q
Output: p \land q

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: y plus z plus x
Output: x + y + z

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: a equals b
Output: a = b

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: q and p
Output: p \land q

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: A intersect B
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: p and q
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a times b
Output: ab

Input: probability of B intersect A
Output: P(A \cap B)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: probability of B union A
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: b times a
Output: ab

Input: B intersect A
Output: A \cap B

Input: matrix A plus matrix B
Output: A + B

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B union A
Output: A \cup B

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: probability of A union B
Output: P(A \cup B)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b i plus a
Output: a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: beta plus alpha
Output: \alpha + \beta

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: expected value of Y plus X
Output: E[X + Y]

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: q and p
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: p or q
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: the summand x plus y
Output: (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: a equals b
Output: a = b

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: B union A
Output: A \cup B

Input: probability of A intersect B
Output: P(A \cap B)

Input: probability of B intersect A
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: q and p
Output: p \land q

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: y plus x
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: q or p
Output: p \lor q

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: B intersect A
Output: A \cap B

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: b equals a
Output: a = b

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: gcd of a and b
Output: \gcd(a,b)

Input: expected value of X plus Y
Output: E[X + Y]

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A intersect B
Output: A \cap B

Input: q and p
Output: p \land q

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z equals b i plus a
Output: z = a + bi

Input: a times b
Output: ab

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: b times a
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: matrix B plus matrix A
Output: A + B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the summand y plus x
Output: (x + y)

Input: A union B
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the summand x plus y
Output: (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the point with coordinates y then x
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: gcd of b and a
Output: \gcd(a,b)

Input: a times b
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a plus b i
Output: a + bi

Input: b equals a
Output: a = b

Input: A intersect B
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand y plus x
Output: (x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: p and q
Output: p \land q

Input: b times c times a
Output: abc

Input: expected value of Y plus X
Output: E[X + Y]

Input: p and q
Output: p \land q

Input: a plus b i
Output: a + bi

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: y plus x
Output: x + y

Input: x plus y
Output: x + y

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: p or q
Output: p \lor q

Input: x squared plus y squared
Output: x^2 + y^2

Input: B union A
Output: A \cup B

Input: p or q
Output: p \lor q

Input: p or q
Output: p \lor q

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: q or p
Output: p \lor q

Input: probability of B union A
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: probability of A intersect B
Output: P(A \cap B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of x plus y
Output: f(x + y)

Input: c times a times b
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: B union A
Output: A \cup B

Input: p or q
Output: p \lor q

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: y plus x
Output: x + y

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: y plus z plus x
Output: x + y + z

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: expected value of Y plus X
Output: E[X + Y]

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: b times c times a
Output: abc

Input: q and p
Output: p \land q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A union B
Output: P(A \cup B)

Input: a equals b
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the summand y plus x
Output: (x + y)

Input: b equals a
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: probability of A intersect B
Output: P(A \cap B)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the point with coordinates x then y
Output: (x,y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: a plus b i
Output: a + bi

Input: probability of A union B
Output: P(A \cup B)

Input: matrix B plus matrix A
Output: A + B

Input: b times a
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the summand y plus x
Output: (x + y)

Input: b times a
Output: ab

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b equals a
Output: a = b

Input: x squared plus y squared
Output: x^2 + y^2

Input: the point with coordinates x then y
Output: (x,y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: a times b
Output: ab

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: p and q
Output: p \land q

Input: b times a
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b equals a
Output: a = b

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x plus y
Output: x + y

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: z equals a plus b i
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: z plus x plus y
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: matrix A plus matrix B
Output: A + B

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: sine of x plus y
Output: \sin(x + y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: z plus x plus y
Output: x + y + z

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: b equals a
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: c times a times b
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: probability of B intersect A
Output: P(A \cap B)

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: beta plus alpha
Output: \alpha + \beta

Input: y plus x
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: z equals a plus b i
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates x then y
Output: (x,y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: c times a times b
Output: abc

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: p or q
Output: p \lor q

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: z plus x plus y
Output: x + y + z

Input: a plus b i
Output: a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the point with coordinates x then y
Output: (x,y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: sine of x plus y
Output: \sin(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: B union A
Output: A \cup B

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix B plus matrix A
Output: A + B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: p or q
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: b equals a
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: sine of y plus x
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand y plus x
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: probability of B union A
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: y plus z plus x
Output: x + y + z

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: z plus x plus y
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a plus b i
Output: a + bi

Input: B intersect A
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the summand x plus y
Output: (x + y)

Input: q or p
Output: p \lor q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: a times b
Output: ab

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: alpha plus beta
Output: \alpha + \beta

Input: matrix B plus matrix A
Output: A + B

Input: sine of x plus y
Output: \sin(x + y)

Input: gcd of b and a
Output: \gcd(a,b)

Input: B union A
Output: A \cup B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: a times b
Output: ab

Input: the summand y plus x
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: matrix A plus matrix B
Output: A + B

Input: y plus z plus x
Output: x + y + z

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: q and p
Output: p \land q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: q and p
Output: p \land q

Input: c times a times b
Output: abc

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: b times a
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b times c times a
Output: abc

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: x plus y
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: b times c times a
Output: abc

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: f of y plus x
Output: f(x + y)

Input: b times c times a
Output: abc

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: matrix A plus matrix B
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: a plus b i
Output: a + bi

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: c times a times b
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of a and b
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b equals a
Output: a = b

Input: f of x plus y
Output: f(x + y)

Input: p or q
Output: p \lor q

Input: y plus z plus x
Output: x + y + z

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: y plus z plus x
Output: x + y + z

Input: B union A
Output: A \cup B

Input: A union B
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: q or p
Output: p \lor q

Input: x plus y
Output: x + y

Input: probability of B intersect A
Output: P(A \cap B)

Input: b times a
Output: ab

Input: b times c times a
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: gcd of b and a
Output: \gcd(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: q or p
Output: p \lor q

Input: b equals a
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a plus b i
Output: a + bi

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b equals a
Output: a = b

Input: y squared plus x squared
Output: x^2 + y^2

Input: sine of x plus y
Output: \sin(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the point with coordinates y then x
Output: (x,y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix A plus matrix B
Output: A + B

Input: z plus x plus y
Output: x + y + z

Input: p and q
Output: p \land q

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: x plus y
Output: x + y

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: c times a times b
Output: abc

Input: A union B
Output: A \cup B

Input: f of x plus y
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the summand x plus y
Output: (x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: A union B
Output: A \cup B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of B intersect A
Output: P(A \cap B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a equals b
Output: a = b

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q or p
Output: p \lor q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: sine of x plus y
Output: \sin(x + y)

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: B intersect A
Output: A \cap B

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: probability of A union B
Output: P(A \cup B)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: sine of x plus y
Output: \sin(x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the point with coordinates y then x
Output: (x,y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A union B
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: the summand y plus x
Output: (x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: c times a times b
Output: abc

Input: x plus y
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: y plus z plus x
Output: x + y + z

Input: a times b
Output: ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: sine of x plus y
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: matrix A plus matrix B
Output: A + B

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: x plus y
Output: x + y

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: y plus x
Output: x + y

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the point with coordinates x then y
Output: (x,y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: q and p
Output: p \land q

Input: A intersect B
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z equals b i plus a
Output: z = a + bi

Input: a equals b
Output: a = b

Input: z equals b i plus a
Output: z = a + bi

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: z equals b i plus a
Output: z = a + bi

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: y plus z plus x
Output: x + y + z

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: A intersect B
Output: A \cap B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y squared plus x squared
Output: x^2 + y^2

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: sine of y plus x
Output: \sin(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: f of x plus y
Output: f(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: y plus x
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: z equals b i plus a
Output: z = a + bi

Input: q or p
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: sine of y plus x
Output: \sin(x + y)

Input: the summand y plus x
Output: (x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of x plus y
Output: \sin(x + y)

Input: a plus b i
Output: a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: a equals b
Output: a = b

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B intersect A
Output: A \cap B

Input: z equals a plus b i
Output: z = a + bi

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b times c times a
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: b times a
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q or p
Output: p \lor q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: expected value of X plus Y
Output: E[X + Y]

Input: gcd of a and b
Output: \gcd(a,b)

Input: a times b
Output: ab

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: q and p
Output: p \land q

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: expected value of Y plus X
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: alpha plus beta
Output: \alpha + \beta

Input: f of x plus y
Output: f(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: a equals b
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: c times a times b
Output: abc

Input: probability of B intersect A
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: expected value of X plus Y
Output: E[X + Y]

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the point with coordinates x then y
Output: (x,y)

Input: a equals b
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: p or q
Output: p \lor q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: a times b
Output: ab

Input: p and q
Output: p \land q

Input: p or q
Output: p \lor q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: p and q
Output: p \land q

Input: a equals b
Output: a = b

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of A intersect B
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B union A
Output: A \cup B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b equals a
Output: a = b

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: f of x plus y
Output: f(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: f of x plus y
Output: f(x + y)

Input: y plus z plus x
Output: x + y + z

Input: z equals b i plus a
Output: z = a + bi

Input: expected value of X plus Y
Output: E[X + Y]

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: sine of y plus x
Output: \sin(x + y)

Input: A union B
Output: A \cup B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: p or q
Output: p \lor q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: q or p
Output: p \lor q

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a equals b
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: sine of y plus x
Output: \sin(x + y)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: z plus x plus y
Output: x + y + z

Input: beta plus alpha
Output: \alpha + \beta

Input: sine of x plus y
Output: \sin(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: a times b
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: matrix B plus matrix A
Output: A + B

Input: the point with coordinates x then y
Output: (x,y)

Input: a equals b
Output: a = b

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: y plus x
Output: x + y

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: x plus y
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: x plus y
Output: x + y

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: B union A
Output: A \cup B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of x plus y
Output: \sin(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: b i plus a
Output: a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z plus x plus y
Output: x + y + z

Input: probability of B union A
Output: P(A \cup B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: A intersect B
Output: A \cap B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: B intersect A
Output: A \cap B

Input: z plus x plus y
Output: x + y + z

Input: B intersect A
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: f of y plus x
Output: f(x + y)

Input: x plus y
Output: x + y

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals a plus b i
Output: z = a + bi

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: b times c times a
Output: abc

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: B intersect A
Output: A \cap B

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times c times a
Output: abc

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: A intersect B
Output: A \cap B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: b equals a
Output: a = b

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the summand x plus y
Output: (x + y)

Input: x squared plus y squared
Output: x^2 + y^2

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a equals b
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: A union B
Output: A \cup B

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A intersect B
Output: A \cap B

Input: B intersect A
Output: A \cap B

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: z equals a plus b i
Output: z = a + bi

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: q or p
Output: p \lor q

Input: z equals b i plus a
Output: z = a + bi

Input: the summand x plus y
Output: (x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times a
Output: ab

Input: z plus x plus y
Output: x + y + z

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: probability of B intersect A
Output: P(A \cap B)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: q and p
Output: p \land q

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of x plus y
Output: f(x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of A intersect B
Output: P(A \cap B)

Input: sine of x plus y
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x squared plus y squared
Output: x^2 + y^2

Input: c times a times b
Output: abc

Input: gcd of a and b
Output: \gcd(a,b)

Input: the summand y plus x
Output: (x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the summand x plus y
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: expected value of Y plus X
Output: E[X + Y]

Input: the point with coordinates x then y
Output: (x,y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: b times c times a
Output: abc

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a equals b
Output: a = b

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the summand y plus x
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: sine of y plus x
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: gcd of a and b
Output: \gcd(a,b)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: probability of A union B
Output: P(A \cup B)

Input: p or q
Output: p \lor q

Input: sine of x plus y
Output: \sin(x + y)

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q or p
Output: p \lor q

Input: the summand x plus y
Output: (x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: A intersect B
Output: A \cap B

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times c times a
Output: abc

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a times b
Output: ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times a
Output: ab

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: x plus y
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q or p
Output: p \lor q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: b i plus a
Output: a + bi

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: a equals b
Output: a = b

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: matrix A plus matrix B
Output: A + B

Input: a times b
Output: ab

Input: y squared plus x squared
Output: x^2 + y^2

Input: b times c times a
Output: abc

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: p and q
Output: p \land q

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: x plus y
Output: x + y

Input: the summand y plus x
Output: (x + y)

Input: f of x plus y
Output: f(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the point with coordinates y then x
Output: (x,y)

Input: b times a
Output: ab

Input: f of x plus y
Output: f(x + y)

Input: f of x plus y
Output: f(x + y)

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: b i plus a
Output: a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: B union A
Output: A \cup B

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: b times a
Output: ab

Input: b times a
Output: ab

Input: B union A
Output: A \cup B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: q and p
Output: p \land q

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: B intersect A
Output: A \cap B

Input: B intersect A
Output: A \cap B

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q or p
Output: p \lor q

Input: probability of A intersect B
Output: P(A \cap B)

Input: z plus x plus y
Output: x + y + z

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: the summand y plus x
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: matrix B plus matrix A
Output: A + B

Input: f of y plus x
Output: f(x + y)

Input: the point with coordinates y then x
Output: (x,y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the point with coordinates x then y
Output: (x,y)

Input: the summand y plus x
Output: (x + y)

Input: the summand y plus x
Output: (x + y)

Input: b times c times a
Output: abc

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: q and p
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: z equals a plus b i
Output: z = a + bi

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: sine of x plus y
Output: \sin(x + y)

Input: c times a times b
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: gcd of b and a
Output: \gcd(a,b)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: a equals b
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: b equals a
Output: a = b

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: y plus x
Output: x + y

Input: expected value of Y plus X
Output: E[X + Y]

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: A intersect B
Output: A \cap B

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: y plus z plus x
Output: x + y + z

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: expected value of Y plus X
Output: E[X + Y]

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: matrix A plus matrix B
Output: A + B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: z plus x plus y
Output: x + y + z

Input: y plus x
Output: x + y

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: matrix A plus matrix B
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b times c times a
Output: abc

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of A union B
Output: P(A \cup B)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: B intersect A
Output: A \cap B

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: probability of B union A
Output: P(A \cup B)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: z equals b i plus a
Output: z = a + bi

Input: f of x plus y
Output: f(x + y)

Input: a plus b i
Output: a + bi

Input: z plus x plus y
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the point with coordinates y then x
Output: (x,y)

Input: a equals b
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: A union B
Output: A \cup B

Input: y plus x
Output: x + y

Input: f of y plus x
Output: f(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: B intersect A
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: p or q
Output: p \lor q

Input: q or p
Output: p \lor q

Input: b i plus a
Output: a + bi

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: a plus b i
Output: a + bi

Input: the summand x plus y
Output: (x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: x plus y
Output: x + y

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of x plus y
Output: f(x + y)

Input: f of y plus x
Output: f(x + y)

Input: z plus x plus y
Output: x + y + z

Input: y plus z plus x
Output: x + y + z

Input: x squared plus y squared
Output: x^2 + y^2

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: sine of y plus x
Output: \sin(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: a times b
Output: ab

Input: z equals a plus b i
Output: z = a + bi

Input: sine of x plus y
Output: \sin(x + y)

Input: b times a
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: z plus x plus y
Output: x + y + z

Input: f of x plus y
Output: f(x + y)

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: probability of B union A
Output: P(A \cup B)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a times b
Output: ab

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: beta plus alpha
Output: \alpha + \beta

Input: f of x plus y
Output: f(x + y)

Input: beta plus alpha
Output: \alpha + \beta

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: b times a
Output: ab

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: B intersect A
Output: A \cap B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: A intersect B
Output: A \cap B

Input: sine of x plus y
Output: \sin(x + y)

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: x plus y
Output: x + y

Input: a plus b i
Output: a + bi

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals a plus b i
Output: z = a + bi

Input: z plus x plus y
Output: x + y + z

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: matrix B plus matrix A
Output: A + B

Input: probability of B union A
Output: P(A \cup B)

Input: y plus z plus x
Output: x + y + z

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: b equals a
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: f of x plus y
Output: f(x + y)

Input: the summand y plus x
Output: (x + y)

Input: b equals a
Output: a = b

Input: expected value of X plus Y
Output: E[X + Y]

Input: x plus y
Output: x + y

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: sine of x plus y
Output: \sin(x + y)

Input: f of x plus y
Output: f(x + y)

Input: b equals a
Output: a = b

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: c times a times b
Output: abc

Input: f of x plus y
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b equals a
Output: a = b

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: matrix B plus matrix A
Output: A + B

Input: x plus y
Output: x + y

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: p and q
Output: p \land q

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: c times a times b
Output: abc

Input: the point with coordinates x then y
Output: (x,y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the summand x plus y
Output: (x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: y plus z plus x
Output: x + y + z

Input: sine of y plus x
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: B union A
Output: A \cup B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z equals b i plus a
Output: z = a + bi

Input: y plus x
Output: x + y

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: c times a times b
Output: abc

Input: beta plus alpha
Output: \alpha + \beta

Input: matrix A plus matrix B
Output: A + B

Input: the quantity x plus y in parentheses
Output: \left(x + y\right)

Input: a equals b
Output: a = b

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: A union B
Output: A \cup B

Input: q or p
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: B union A
Output: A \cup B

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a equals b
Output: a = b

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: z equals b i plus a
Output: z = a + bi

Input: probability of B union A
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: b times c times a
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: q or p
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: q and p
Output: p \land q

Input: p and q
Output: p \land q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: z equals a plus b i
Output: z = a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: expected value of X plus Y
Output: E[X + Y]

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: p and q
Output: p \land q

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: A intersect B
Output: A \cap B

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: b times a
Output: ab

Input: a times b
Output: ab

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: x plus y
Output: x + y

Input: a equals b
Output: a = b

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: y plus x
Output: x + y

Input: a times b
Output: ab

Input: expected value of X plus Y
Output: E[X + Y]

Input: the point with coordinates y then x
Output: (x,y)

Input: the point with coordinates y then x
Output: (x,y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: x squared plus y squared
Output: x^2 + y^2

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: b i plus a
Output: a + bi

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: matrix A plus matrix B
Output: A + B

Input: f of x plus y
Output: f(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: alpha plus beta
Output: \alpha + \beta

Input: a times b
Output: ab

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: sine of x plus y
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: sine of y plus x
Output: \sin(x + y)

Input: B intersect A
Output: A \cap B

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: x plus y
Output: x + y

Input: sine of y plus x
Output: \sin(x + y)

Input: a times b
Output: ab

Input: matrix B plus matrix A
Output: A + B

Input: p and q
Output: p \land q

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: gcd of a and b
Output: \gcd(a,b)

Input: x plus y
Output: x + y

Input: matrix B plus matrix A
Output: A + B

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the summand y plus x
Output: (x + y)

Input: c times a times b
Output: abc

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y plus z plus x
Output: x + y + z

Input: y plus x
Output: x + y

Input: probability of A intersect B
Output: P(A \cap B)

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p or q
Output: p \lor q

Input: the set containing b a and c with braces
Output: \left\{ a, b, c \right\}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: matrix B plus matrix A
Output: A + B

Input: probability of B intersect A
Output: P(A \cap B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: probability of B intersect A
Output: P(A \cap B)

Input: the summand x plus y
Output: (x + y)

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the summand x plus y
Output: (x + y)

Input: c times a times b
Output: abc

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of A union B
Output: P(A \cup B)

Input: x plus y
Output: x + y

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: y plus z plus x
Output: x + y + z

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: y plus x
Output: x + y

Input: B intersect A
Output: A \cap B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals b i plus a
Output: z = a + bi

Input: y plus z plus x
Output: x + y + z

Input: A intersect B
Output: A \cap B

Input: sine of y plus x
Output: \sin(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: matrix A plus matrix B
Output: A + B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: matrix B plus matrix A
Output: A + B

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: b equals a
Output: a = b

Input: a equals b
Output: a = b

Input: y plus x
Output: x + y

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: f of y plus x
Output: f(x + y)

Input: b equals a
Output: a = b

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: probability of A intersect B
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: c times a times b
Output: abc

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: b times c times a
Output: abc

Input: b equals a
Output: a = b

Input: p or q
Output: p \lor q

Input: a equals b
Output: a = b

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: sine of x plus y
Output: \sin(x + y)

Input: B union A
Output: A \cup B

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: matrix A plus matrix B
Output: A + B

Input: the summand y plus x
Output: (x + y)

Input: p or q
Output: p \lor q

Input: B union A
Output: A \cup B

Input: B union A
Output: A \cup B

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: z plus x plus y
Output: x + y + z

Input: f of y plus x
Output: f(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: the point with coordinates y then x
Output: (x,y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: a equals b
Output: a = b

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: b times a
Output: ab

Input: probability of A union B
Output: P(A \cup B)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of y plus x
Output: f(x + y)

Input: p and q
Output: p \land q

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the summand y plus x
Output: (x + y)

Input: p or q
Output: p \lor q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: a plus b i
Output: a + bi

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: B union A
Output: A \cup B

Input: a equals b
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: y plus x
Output: x + y

Input: q or p
Output: p \lor q

Input: q and p
Output: p \land q

Input: a plus b i
Output: a + bi

Input: a equals b
Output: a = b

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: f of x plus y
Output: f(x + y)

Input: a times b
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: a times b
Output: ab

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: probability of B intersect A
Output: P(A \cap B)

Input: the equation y squared plus x squared equals r squared
Output: x^2 + y^2 = r^2

Input: q and p
Output: p \land q

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: a equals b
Output: a = b

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a times b
Output: ab

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: matrix A plus matrix B
Output: A + B

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: x plus y
Output: x + y

Input: A union B
Output: A \cup B

Input: b times a
Output: ab

Input: probability of B union A
Output: P(A \cup B)

Input: the summand x plus y
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: b times c times a
Output: abc

Input: b times c times a
Output: abc

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: f of x plus y
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: y squared plus x squared
Output: x^2 + y^2

Input: p and q
Output: p \land q

Input: the divergence of G plus F
Output: \nabla \cdot (F + G)

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z plus x plus y
Output: x + y + z

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: z plus x plus y
Output: x + y + z

Input: q and p
Output: p \land q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: beta plus alpha
Output: \alpha + \beta

Input: matrix B plus matrix A
Output: A + B

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b equals a
Output: a = b

Input: A union B
Output: A \cup B

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: probability of B union A
Output: P(A \cup B)

Input: sine of y plus x
Output: \sin(x + y)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: y plus z plus x
Output: x + y + z

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: B union A
Output: A \cup B

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: z plus x plus y
Output: x + y + z

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: c times a times b
Output: abc

Input: B intersect A
Output: A \cap B

Input: p or q
Output: p \lor q

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: gcd of a and b
Output: \gcd(a,b)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y squared plus x squared
Output: x^2 + y^2

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: q or p
Output: p \lor q

Input: q or p
Output: p \lor q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: a equals b
Output: a = b

Input: the summand y plus x
Output: (x + y)

Input: b times c times a
Output: abc

Input: q or p
Output: p \lor q

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: absolute value of y minus x
Output: \left|x - y\right|

Input: y plus z plus x
Output: x + y + z

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: z plus x plus y
Output: x + y + z

Input: the summand x plus y
Output: (x + y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: p or q
Output: p \lor q

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: a times b
Output: ab

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b times a
Output: ab

Input: matrix A plus matrix B
Output: A + B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x squared plus y squared
Output: x^2 + y^2

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: c times a times b
Output: abc

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: A intersect B
Output: A \cap B

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a equals b
Output: a = b

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: A union B
Output: A \cup B

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of A union B
Output: P(A \cup B)

Input: expected value of X plus Y
Output: E[X + Y]

Input: probability of B union A
Output: P(A \cup B)

Input: probability of B union A
Output: P(A \cup B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the product of a times b from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: B intersect A
Output: A \cap B

Input: f of x plus y
Output: f(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: b i plus a
Output: a + bi

Input: b i plus a
Output: a + bi

Input: y plus x
Output: x + y

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: f of y plus x
Output: f(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p or q
Output: p \lor q

Input: z plus x plus y
Output: x + y + z

Input: x plus y
Output: x + y

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: y plus z plus x
Output: x + y + z

Input: the point with coordinates x then y
Output: (x,y)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: b i plus a
Output: a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: c times a times b
Output: abc

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: f of x plus y
Output: f(x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the summand x plus y
Output: (x + y)

Input: sine of y plus x
Output: \sin(x + y)

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: c times a times b
Output: abc

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: matrix B plus matrix A
Output: A + B

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: probability of A union B
Output: P(A \cup B)

Input: f of y plus x
Output: f(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: probability of A intersect B
Output: P(A \cap B)

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: p or q
Output: p \lor q

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: sine of y plus x
Output: \sin(x + y)

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: a equals b
Output: a = b

Input: the curl of G plus F
Output: \nabla \times (F + G)

Input: y plus x
Output: x + y

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: z equals b i plus a
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: z equals b i plus a
Output: z = a + bi

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: z plus x plus y
Output: x + y + z

Input: a times b
Output: ab

Input: the curl of F plus G
Output: \nabla \times (F + G)

Input: x plus y
Output: x + y

Input: a times b
Output: ab

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: the summand y plus x
Output: (x + y)

Input: choose j from n plus choose k from n
Output: \binom{n}{j} + \binom{n}{k}

Input: p or q
Output: p \lor q

Input: z equals a plus b i
Output: z = a + bi

Input: p or q
Output: p \lor q

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: sine of x plus y
Output: \sin(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the summand x plus y
Output: (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: q and p
Output: p \land q

Input: lcm of b and a
Output: \operatorname{lcm}(a,b)

Input: q or p
Output: p \lor q

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: x plus y
Output: x + y

Input: lcm of a and b
Output: \operatorname{lcm}(a,b)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: probability of B union A
Output: P(A \cup B)

Input: b times c times a
Output: abc

Input: the summand y plus x
Output: (x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: y plus x
Output: x + y

Input: q or p
Output: p \lor q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: b times c times a
Output: abc

Input: probability of B union A
Output: P(A \cup B)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: A intersect B
Output: A \cap B

Input: y plus z plus x
Output: x + y + z

Input: gcd of b and a
Output: \gcd(a,b)

Input: c times a times b
Output: abc

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the summand x plus y
Output: (x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: the equation x squared plus y squared equals r squared
Output: x^2 + y^2 = r^2

Input: b times a
Output: ab

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: probability of A intersect B
Output: P(A \cap B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the point with coordinates x then y
Output: (x,y)

Input: a equals b
Output: a = b

Input: b equals a
Output: a = b

Input: p and q
Output: p \land q

Input: y plus x
Output: x + y

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: p or q
Output: p \lor q

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: x plus y
Output: x + y

Input: z equals a plus b i
Output: z = a + bi

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: b i plus a
Output: a + bi

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: b equals a
Output: a = b

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: gamma of y plus gamma of x
Output: \Gamma(x) + \Gamma(y)

Input: expected value of Y plus X
Output: E[X + Y]

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: sine of y plus x
Output: \sin(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: x plus y
Output: x + y

Input: b times a
Output: ab

Input: y plus z plus x
Output: x + y + z

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: z equals a plus b i
Output: z = a + bi

Input: b times a
Output: ab

Input: A intersect B
Output: A \cap B

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: matrix A plus matrix B
Output: A + B

Input: the summand x plus y
Output: (x + y)

Input: z plus x plus y
Output: x + y + z

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: expected value of X plus Y
Output: E[X + Y]

Input: matrix A plus matrix B
Output: A + B

Input: variance of Y plus X
Output: \mathrm{Var}(X + Y)

Input: q and p
Output: p \land q

Input: A union B
Output: A \cup B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: absolute value of x minus y
Output: \left|x - y\right|

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the set containing elements 1 2 and 3
Output: \{ 1, 2, 3 \}

Input: B union A
Output: A \cup B

Input: sine of x plus y
Output: \sin(x + y)

Input: a equals b
Output: a = b

Input: probability of A union B
Output: P(A \cup B)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of A intersect B
Output: P(A \cap B)

Input: z equals a plus b i
Output: z = a + bi

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: the summand y plus x
Output: (x + y)

Input: f of y plus x
Output: f(x + y)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: y plus x
Output: x + y

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: gcd of b and a
Output: \gcd(a,b)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times a
Output: ab

Input: sine of x plus y
Output: \sin(x + y)

Input: choose k from n plus choose j from n
Output: \binom{n}{j} + \binom{n}{k}

Input: the divergence of F plus G
Output: \nabla \cdot (F + G)

Input: A union B
Output: A \cup B

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: z equals b i plus a
Output: z = a + bi

Input: q and p
Output: p \land q

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: y plus z plus x
Output: x + y + z

Input: matrix B plus matrix A
Output: A + B

Input: the set with elements y x and z
Output: \{ x, y, z \}

Input: matrix B plus matrix A
Output: A + B

Input: the sum of x plus y from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: a times b
Output: ab

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: p and q
Output: p \land q

Input: q and p
Output: p \land q

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: matrix A plus matrix B
Output: A + B

Input: c times a times b
Output: abc

Input: y plus x
Output: x + y

Input: the modulus of y plus x i
Output: \left|x i + y\right|

Input: matrix B plus matrix A
Output: A + B

Input: the set containing elements 3 1 and 2
Output: \{ 1, 2, 3 \}

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: expected value of Y plus X
Output: E[X + Y]

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: a equals b
Output: a = b

Input: x plus y
Output: x + y

Input: x plus y
Output: x + y

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: p and q
Output: p \land q

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of B union A
Output: P(A \cup B)

Input: vector y plus vector x
Output: \mathbf{x} + \mathbf{y}

Input: probability of B union A
Output: P(A \cup B)

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: b times c times a
Output: abc

Input: a equals b
Output: a = b

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: b times c times a
Output: abc

Input: a times b
Output: ab

Input: gamma of x plus gamma of y
Output: \Gamma(x) + \Gamma(y)

Input: b equals a
Output: a = b

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: f of y plus x
Output: f(x + y)

Input: vector a dot vector b
Output: \mathbf{a}\cdot \mathbf{b}

Input: b equals a
Output: a = b

Input: probability of B intersect A
Output: P(A \cap B)

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the point with coordinates y then x
Output: (x,y)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: c times a times b
Output: abc

Input: a times b
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: y plus z plus x
Output: x + y + z

Input: the set containing c b and a with braces
Output: \left\{ a, b, c \right\}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the point with coordinates x then y
Output: (x,y)

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: a times b
Output: ab

Input: the set with elements z y and x
Output: \{ x, y, z \}

Input: x plus y
Output: x + y

Input: c times a times b
Output: abc

Input: b times a
Output: ab

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: b i plus a
Output: a + bi

Input: a congruent to b mod n
Output: a \equiv b \pmod{n}

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: the gradient of g plus f
Output: \nabla(f + g)

Input: p and q
Output: p \land q

Input: the limit of y plus x as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: the set containing vectors b a and c
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: f of y plus x
Output: f(x + y)

Input: q and p
Output: p \land q

Input: b i plus a
Output: a + bi

Input: q or p
Output: p \lor q

Input: p and q
Output: p \land q

Input: the gradient of f plus g
Output: \nabla(f + g)

Input: z plus x plus y
Output: x + y + z

Input: b congruent to a mod n
Output: a \equiv b \pmod{n}

Input: q and p
Output: p \land q

Input: x plus y
Output: x + y

Input: the set containing vectors c b and a
Output: \{ \mathbf{a}, \mathbf{b}, \mathbf{c} \}

Input: the set containing elements c b a
Output: \{ a, b, c \}

Input: the integral of y plus x with respect to x
Output: \int (x + y)\,dx

Input: A intersect B
Output: A \cap B

Input: f of y plus x
Output: f(x + y)

Input: the integral of x plus y with respect to x
Output: \int (x + y)\,dx

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand y plus x
Output: (x + y)

Input: sine of x plus y
Output: \sin(x + y)

Input: the derivative of x plus y
Output: \frac{d}{dx}(x + y)

Input: the derivative of y plus x
Output: \frac{d}{dx}(x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the inner product of a and b
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the modulus of x i plus y
Output: \left|x i + y\right|

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: the limit of x plus y as x approaches zero
Output: \lim_{x\to 0}(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: the dot product of vector b and vector a
Output: \mathbf{a}\cdot \mathbf{b}

Input: a times b
Output: ab

Input: y plus x
Output: x + y

Input: b i plus a
Output: a + bi

Input: the product of b times a from k equals one to n
Output: \prod_{k=1}^{n} ab

Input: sine of y plus x
Output: \sin(x + y)

Input: matrix A plus matrix B
Output: A + B

Input: x plus y
Output: x + y

Input: y squared plus x squared
Output: x^2 + y^2

Input: b times c times a
Output: abc

Input: q or p
Output: p \lor q

Input: f of x plus y
Output: f(x + y)

Input: q and p
Output: p \land q

Input: q or p
Output: p \lor q

Input: vector x plus vector y
Output: \mathbf{x} + \mathbf{y}

Input: sine of y plus x
Output: \sin(x + y)

Input: probability of A union B
Output: P(A \cup B)

Input: the point with coordinates y then x
Output: (x,y)

Input: p or q
Output: p \lor q

Input: sine of y plus x
Output: \sin(x + y)

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: probability of B intersect A
Output: P(A \cap B)

Input: variance of X plus Y
Output: \mathrm{Var}(X + Y)

Input: b equals a
Output: a = b

Input: a times b
Output: ab

Input: a times b
Output: ab

Input: x squared plus y squared
Output: x^2 + y^2

Input: B intersect A
Output: A \cap B

Input: the set containing elements b a and c
Output: \{ a, b, c \}

Input: expected value of Y plus X
Output: E[X + Y]

Input: the sum of y plus x from i equals one to n
Output: \sum_{i=1}^{n} (x + y)

Input: the inner product of b and a
Output: \langle \mathbf{a}, \mathbf{b}\rangle

Input: the summand x plus y
Output: (x + y)

Input: matrix A plus matrix B
Output: A + B
