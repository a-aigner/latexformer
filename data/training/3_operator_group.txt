Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides b plus c
Output: a \mid b + c

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides b plus c
Output: a \mid b + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides b plus c
Output: a \mid b + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides b plus c
Output: a \mid b + c

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides b plus c
Output: a \mid b + c

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides b plus c
Output: a \mid b + c

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides b plus c
Output: a \mid b + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divides b plus c
Output: a \mid b + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: a divides b plus c
Output: a \mid b + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides b plus c
Output: a \mid b + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides b plus c
Output: a \mid b + c

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divides b plus c
Output: a \mid b + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides b plus c
Output: a \mid b + c

Input: a divides b plus c
Output: a \mid b + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a minus b times c
Output: a - bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus the product of b and c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the square of a plus b
Output: a^2 + b

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: a divides b plus c
Output: a \mid b + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: a divides b plus c
Output: a \mid b + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divides b plus c
Output: a \mid b + c

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides b plus c
Output: a \mid b + c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a minus b times c
Output: a - bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of a plus b
Output: a^2 + b

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times c
Output: a + bc

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the real part of z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divides b plus c
Output: a \mid b + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of f squared
Output: \nabla f^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a intersect b
Output: x \in A \cap B

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a union b
Output: x \in A \cup B

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: x in a union b
Output: x \in A \cup B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: x in a union b
Output: x \in A \cup B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a minus b times c
Output: a - bc

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: a minus b times c
Output: a - bc

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: a plus the product of b and c
Output: a + bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the real part of z squared
Output: \Re(z^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus the product of b and c
Output: a + bc

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the modulus of z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of a plus b
Output: a^2 + b

Input: x in a union b
Output: x \in A \cup B

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times c
Output: a + bc

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of a plus b
Output: a^2 + b

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the modulus of z plus one
Output: |z + 1|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of z squared
Output: \Re(z^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the product of a plus b and c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the square of a plus b
Output: a^2 + b

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus the product of b and c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of a plus b
Output: a^2 + b

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of f squared
Output: \nabla f^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides b plus c
Output: a \mid b + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the modulus of z plus one
Output: |z + 1|

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times c
Output: a + bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a plus b times c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times c
Output: a + bc

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: x in a union b
Output: x \in A \cup B

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: x in a union b
Output: x \in A \cup B

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a minus b times c
Output: a - bc

Input: the quantity a minus b times c
Output: (a - b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the modulus of z plus one
Output: |z + 1|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divides b plus c
Output: a \mid b + c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a union b
Output: x \in A \cup B

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the quantity a plus b times c
Output: (a + b)c

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the real part of z squared
Output: \Re(z^2)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus the product of b and c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a intersect b
Output: x \in A \cap B

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the quantity a minus b times c
Output: (a - b)c

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the product of a plus b and c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the gradient of f squared
Output: \nabla f^2

Input: the real part of z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the real part of z squared
Output: \Re(z^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides b plus c
Output: a \mid b + c

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of z plus one
Output: |z + 1|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of a plus b
Output: a^2 + b

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: a divides b plus c
Output: a \mid b + c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: x in a union b
Output: x \in A \cup B

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: x in a intersect b
Output: x \in A \cap B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: x in a union b
Output: x \in A \cup B

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: x in a union b
Output: x \in A \cup B

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a plus the product of b and c
Output: a + bc

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: x in a intersect b
Output: x \in A \cap B

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a plus b times c
Output: (a + b)c

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of a plus b
Output: a^2 + b

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: x in a intersect b
Output: x \in A \cap B

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: x in a intersect b
Output: x \in A \cap B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the square of a plus b
Output: a^2 + b

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the square of a plus b
Output: a^2 + b

Input: a minus b times c
Output: a - bc

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of z plus one
Output: |z + 1|

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: x in a intersect b
Output: x \in A \cap B

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a minus b times c
Output: a - bc

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a minus b times c
Output: a - bc

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the gradient of f squared
Output: \nabla f^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the quantity a plus b times c
Output: (a + b)c

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of f squared
Output: \nabla f^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the modulus of z plus one
Output: |z + 1|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a intersect b
Output: x \in A \cap B

Input: x in a union b
Output: x \in A \cup B

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity a plus b times c
Output: (a + b)c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the modulus of z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divides b plus c
Output: a \mid b + c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the real part of z squared
Output: \Re(z^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of a plus b
Output: a^2 + b

Input: the real part of z squared
Output: \Re(z^2)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: x in a union b
Output: x \in A \cup B

Input: a minus b times c
Output: a - bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the product of a plus b and c
Output: (a + b)c

Input: a divides b plus c
Output: a \mid b + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the real part of z squared
Output: \Re(z^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the gradient of f squared
Output: \nabla f^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides b plus c
Output: a \mid b + c

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the quantity a plus b times c
Output: (a + b)c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of a plus b
Output: a^2 + b

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: x in a union b
Output: x \in A \cup B

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: x in a union b
Output: x \in A \cup B

Input: the gradient of f squared
Output: \nabla f^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a intersect b
Output: x \in A \cap B

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the modulus of z plus one
Output: |z + 1|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: a minus b times c
Output: a - bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: x in a union b
Output: x \in A \cup B

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the distance between a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: x in a union b
Output: x \in A \cup B

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: a minus b times c
Output: a - bc

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of A plus B
Output: \det(A + B)

Input: x in a union b
Output: x \in A \cup B

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the determinant of A plus B
Output: \det(A + B)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the square of a plus b
Output: a^2 + b

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of a plus b
Output: a^2 + b

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the square of a plus b
Output: a^2 + b

Input: a divides b plus c
Output: a \mid b + c

Input: x in a union b
Output: x \in A \cup B

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the gradient of f squared
Output: \nabla f^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a plus the product of b and c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the real part of z squared
Output: \Re(z^2)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the modulus of z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: a minus b times c
Output: a - bc

Input: a minus b times c
Output: a - bc

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of z squared
Output: \Re(z^2)

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the quantity a minus b times c
Output: (a - b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of z plus one
Output: |z + 1|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: a minus b times c
Output: a - bc

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the gradient of f squared
Output: \nabla f^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a plus b times c
Output: a + bc

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: x in a union b
Output: x \in A \cup B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a divides b plus c
Output: a \mid b + c

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a minus b times c
Output: a - bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the absolute value of the quantity a over b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a plus b times c
Output: (a + b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: a plus b times c
Output: a + bc

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and c
Output: (a + b)c

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides b plus c
Output: a \mid b + c

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus the product of b and c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the determinant of A plus B
Output: \det(A + B)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the gradient of f squared
Output: \nabla f^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the product of a plus b and c
Output: (a + b)c

Input: a minus b times c
Output: a - bc

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of f squared
Output: \nabla f^2

Input: the gradient of f squared
Output: \nabla f^2

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the product of a plus b and c
Output: (a + b)c

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the gamma of x plus one
Output: \Gamma(x + 1)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: x in a union b
Output: x \in A \cup B

Input: the square of a plus b
Output: a^2 + b

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a congruent to the quantity b plus c mod n
Output: a \equiv (b + c) \pmod{n}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: a plus b times c
Output: a + bc

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: x in a union b
Output: x \in A \cup B

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a intersect b
Output: x \in A \cap B

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the product of a plus b and c
Output: (a + b)c

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: a plus the product of b and c
Output: a + bc

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: a divides b plus c
Output: a \mid b + c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the curl of f cross g plus h
Output: \nabla \times (F \times G + H)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the determinant of A plus B
Output: \det(A + B)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus the product of b and c
Output: a + bc

Input: the real part of z squared
Output: \Re(z^2)

Input: the square of a plus b
Output: a^2 + b

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the quantity a plus b times c
Output: (a + b)c

Input: x in a union b
Output: x \in A \cup B

Input: the modulus of z plus one
Output: |z + 1|

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the divergence of the quantity f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of z squared
Output: \Re(z^2)

Input: a plus b times c
Output: a + bc

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: expected value of the quantity x squared plus one
Output: E[(X^2 + 1)]

Input: the product of a plus b and c
Output: (a + b)c

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the determinant of A plus B
Output: \det(A + B)

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: a minus b times c
Output: a - bc

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the product of a plus b and c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a plus b times c
Output: a + bc

Input: the determinant of A plus B
Output: \det(A + B)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: x in a union b
Output: x \in A \cup B

Input: x in a union b
Output: x \in A \cup B

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the absolute value of a over the quantity b plus c
Output: \left|\frac{a}{b + c}\right|

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: x in a intersect b
Output: x \in A \cap B

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and c
Output: (a + b)c

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: the product of a plus b and c
Output: (a + b)c

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the limit of sin x over the quantity x plus one
Output: \lim \frac{\sin x}{x + 1}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: x in a intersect b
Output: x \in A \cap B

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of a plus b
Output: a^2 + b

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the product of a plus b and c
Output: (a + b)c

Input: the modulus of z plus one
Output: |z + 1|

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the gradient of f squared
Output: \nabla f^2

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: the quantity a plus b times c
Output: (a + b)c

Input: a plus b times c
Output: a + bc

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the distance between a plus b and c
Output: |a + b - c|

Input: a plus b times c
Output: a + bc

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the modulus of the quantity z plus one
Output: |z + 1|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: a divides the quantity b plus c
Output: a \mid (b + c)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the quantity a plus b times c
Output: (a + b)c

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: a minus b times c
Output: a - bc

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the determinant of A plus B
Output: \det(A + B)

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the square of a plus b
Output: a^2 + b

Input: the set of x such that x is in a intersect b
Output: \{x \mid x \in A \cap B\}

Input: a minus b times c
Output: a - bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: a plus b times c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the number of subsets of a set of size the quantity n plus one
Output: 2^{n + 1}

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the quantity a plus b times c
Output: (a + b)c

Input: the square of a plus b
Output: a^2 + b

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the square of the distance between x and y
Output: (x - y)^2

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the integral of x squared plus the constant one
Output: \int (x^2 + 1)\,dx

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the probability of the quantity a and b given c
Output: P((A \cap B) \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: the square of the quantity x minus y
Output: (x - y)^2

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the square of a plus b
Output: a^2 + b

Input: a plus the product of b and c
Output: a + bc

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the quantity a minus b times c
Output: (a - b)c

Input: the curl of the quantity f cross g plus h
Output: \nabla \times (F \times G + H)

Input: the modulus of z plus one
Output: |z + 1|

Input: the product of matrix A plus B and vector x
Output: (A + B)\mathbf{x}

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: a congruent to b plus c mod n
Output: a \equiv b + c \pmod{n}

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: a plus b times vector x
Output: a + b\mathbf{x}

Input: a plus the product of b and c
Output: a + bc

Input: the quantity x in a union b
Output: x \in (A \cup B)

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: a divided by b plus c
Output: \frac{a}{b} + c

Input: the square of a plus b
Output: a^2 + b

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the probability of a and b given c
Output: P(A \cap B \mid C)

Input: the product of a plus b and c
Output: (a + b)c

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: A plus B times vector x
Output: A + B\mathbf{x}

Input: the determinant of the quantity A plus B
Output: \det(A + B)

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: a plus b times c
Output: a + bc

Input: not p implies q
Output: \neg P \Rightarrow Q

Input: the integral of x squared plus one
Output: \int (x^2 + 1)\,dx

Input: the quantity a minus b times c
Output: (a - b)c

Input: the square of the distance between x and y
Output: (x - y)^2

Input: x in a intersect b
Output: x \in A \cap B

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the distance between the quantity a plus b and c
Output: |a + b - c|

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: the square of the quantity a plus b
Output: (a + b)^2

Input: the number of subsets of a set of size n plus one
Output: 2^{n + 1}

Input: the derivative of the quantity sin x squared
Output: \frac{d}{dx}(\sin x)^2

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the negation of the quantity p implies q
Output: \neg(P \Rightarrow Q)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: n choose k plus one
Output: \binom{n}{k} + 1

Input: the sum of i squared plus j squared
Output: \sum (i^2 + j^2)

Input: expected value of x squared plus one
Output: E[X^2 + 1]

Input: the product of a plus b and vector x
Output: (a + b)\mathbf{x}

Input: the quantity a minus b times c
Output: (a - b)c

Input: a plus the product of b and c
Output: a + bc

Input: the distance between a plus b and c
Output: |a + b - c|

Input: the gradient of the quantity f squared
Output: \nabla(f^2)

Input: the limit of sin x over x plus one
Output: \lim \frac{\sin x}{x} + 1

Input: the real part of the quantity z squared
Output: \Re(z^2)

Input: the sum of i squared plus one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the square of a plus b
Output: a^2 + b

Input: the derivative of sin x squared
Output: \frac{d}{dx}\sin x^2

Input: the sum of the quantity i squared plus j squared
Output: \sum (i^2 + j^2)

Input: the gamma of the quantity x plus one
Output: \Gamma(x + 1)

Input: the sum of i squared plus the constant one from i equals one to n
Output: \sum_{i=1}^n (i^2 + 1)

Input: the quantity n choose k plus one
Output: (\binom{n}{k}) + 1

Input: a divided by the sum of b and c
Output: \frac{a}{b + c}

Input: the divergence of f plus g
Output: \nabla \cdot (F + G)
